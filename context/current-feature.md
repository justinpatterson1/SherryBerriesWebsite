# Current Feature: Dynamic Sitemap and Robots Configuration

## Status
In Progress

## Goals
- `src/app/sitemap.ts` exists and Next.js serves valid XML at `/sitemap.xml`.
- `src/app/robots.ts` exists and Next.js serves valid robots.txt at `/robots.txt`, referencing the sitemap.
- Base URL resolves to `https://shopsherryberries.com` in production, reusing `NEXT_PUBLIC_SITE_URL` rather than adding a new env var, normalized so no URL has a doubled `/`.
- Real public routes only — every static entry verified to exist in `src/app/`, none invented from the PRD's example list.
- Active product pages generated from Prisma at the canonical `/products/[slug]`, using the real active field and `updatedAt` for `lastModified`.
- No `?category=` entries — `/products` is the only listing URL, since categories are filters rather than routes.
- `metadataBase` set on the root layout to the canonical apex, so canonical and OG URLs stop resolving relative and match the sitemap.
- Private, transactional and utility routes excluded from the sitemap and disallowed in robots: `/admin`, `/api`, `/account`, `/checkout`, `/cart`, `/order`, `/login`, `/forgot-password`, `/reset-password`, `/verify-email`, `/unsubscribe`, `/wishlist`.
- Vercel preview deployments are not indexable, without blocking production.
- A database failure degrades to the static entries instead of breaking the endpoint, logged through Sentry rather than swallowed.
- No duplicate URLs; no new dependency; `npm run lint` and `npm run build` pass.

## Notes
Spec: [context/features/sitemap-robot-spec.md](features/sitemap-robot-spec.md).

This closes the "no sitemap.ts, no robots.ts" item from the pre-launch review.

**PRD examples are not this codebase.** The PRD lists routes like `/shop`, `/about`, `/faq`, `/aftercare`, `/returns` and category URLs shaped `/shop/belly-rings`. This project does not use those. The real public routes are `/`, `/products`, `/bestsellers`, `/our-story`, `/contact`, `/help/shipping`, `/help/returns`, `/learn/sizing`, `/privacy`, `/terms`. The PRD says explicitly not to create URLs for routes that do not exist, so the inspection findings win over its examples.

**Categories are a query string, not a path — decided 2026-09-12: list `/products` alone.** The homepage grid and navbar link to `/products?category={slug}` ([queries/home.ts](../src/lib/queries/home.ts), [layout/navbar.tsx](../src/components/layout/navbar.tsx)); there is no `/shop/belly-rings` style route. §7 asks for category pages and §8 excludes filter-query URLs, and here they are the same URLs. The sitemap therefore carries `/products` and no `?category=` entries, which also satisfies §8's "no duplicate URLs" and "no filter query URLs" — every product is already reachable through its own `/products/[slug]` entry. Revisit only if categories ever get real path-based routes.

**`metadataBase` — decided 2026-09-12: set it site-wide, in this feature.** It is absent everywhere today, so the site emits no canonical tags and every OG/Twitter image URL resolves relative. Set on the root layout's `metadata` export to the canonical apex below, which satisfies §17 (sitemap URLs matching the site's canonical strategy) and closes the related pre-launch item in one pass.

**Prisma fields to use (verified, do not guess):** `Product.active` (Boolean), `Product.slug`, `Product.updatedAt`; `Category.slug`. The product page itself 404s on `!product || !product.active` ([products/[slug]/page.tsx:52](../src/app/products/[slug]/page.tsx#L52)), so the sitemap filter must match that exactly.

**Env caution.** `NEXT_PUBLIC_*` is inlined at build time, as [.env.example](../.env.example) already warns. Preview detection should use `VERCEL_ENV`, which is available server-side at request time.

**Canonical host — decided 2026-09-12: the apex, `https://shopsherryberries.com`.** `www` 301-redirects to it in Vercel's domain settings. The sitemap, canonical tags and every generated link use this host.

**⚠ Pre-existing bug found while loading this spec — `NEXT_PUBLIC_SITE_URL` must carry its scheme.** The owner's value was `www.shopsherryberries.com`, with no `https://`. The variable is concatenated, never parsed:

- [checkout/route.ts:233-239](../src/app/api/checkout/route.ts#L233-L239) builds WiPay's `response_url` as `` `${baseUrl}/api/checkout/wipay/return` ``. A bare host makes that relative; WiPay requires an absolute `response_url`, so the payor is never redirected back and a real charge leaves the order `PENDING`.
- [admin/payments/route.ts:202](../src/app/api/admin/payments/route.ts#L202) builds the link in the payment-rejected email the same way, producing a dead link.

[.env.example](../.env.example) now documents the constraint and ships the correct value. **The real `.env` and the Vercel environment variable still need updating**, and because `NEXT_PUBLIC_*` is build-time inlined, Vercel needs a redeploy after the change. This is a launch blocker in its own right, independent of the sitemap.
---

## History
- 2026-08-16 — **Admin-managed jewelry categories** → [src/app/api/admin/categories/route.ts](../src/app/api/admin/categories/route.ts), [src/components/admin/categories-view.tsx](../src/components/admin/categories-view.tsx), [src/lib/admin/options.ts](../src/lib/admin/options.ts) + [options.test.ts](../src/lib/admin/options.test.ts), and edits to [admin-client.tsx](../src/components/admin/admin-client.tsx), [product-form.tsx](../src/components/admin/product-form.tsx), [shared.tsx](../src/components/admin/shared.tsx), [queries/admin.ts](../src/lib/queries/admin.ts), [queries/product.ts](../src/lib/queries/product.ts), [queries/home.ts](../src/lib/queries/home.ts), [home/categories.tsx](../src/components/home/categories.tsx). Categories were the storefront's filtering dimension with **no admin UI at all** — they existed only because the seed created them. **Decisions (locked at `start`, taken as defaults rather than asked, since the owner re-ran `start` instead of answering):** (1) **category CRUD only, no migration** — `Product.jewelryType` stays a Prisma enum a developer extends, and a new category borrows an existing value exactly as the seed already does (`accessories` and `merch` are both filed under the `AFTERCARE` catch-all); (2) a **Categories tab** in `/admin` beside Inventory, matching the existing view structure so the image and SEO fields have somewhere to live; (3) **delete blocked** while products reference the category. The two rejected options — promoting `JewelryType` to a table, or collapsing it into `Category` — remain the honest long-term model and are written up in the loaded spec (see git history for this file). **API** ([route.ts](../src/app/api/admin/categories/route.ts)): `POST`/`PATCH`/`DELETE` behind `requireAdmin`; the slug is derived from the name when left blank and **re-slugified server-side regardless**, so a hand-typed value is normalised rather than trusted; uniqueness clashes return 409 naming the URL at fault; `DELETE` refuses with a 409 and a product count, since `Product.categoryId` is required with **no `onDelete`** and the database would reject it anyway — this turns a 500 into a sentence worth reading. **UI**: list rows show thumbnail, live `/products?category=<slug>`, description, product count and returns classification; add/edit modal covers description, image (reusing `/api/admin/upload`) and a collapsed SEO section; the slug field previews the customer-facing URL as you type and **warns when an edit would break links already shared**. **The product form now reads categories from live state** rather than the server payload, so a category created in the Categories tab is selectable immediately with no reload — that was the specific goal of the request. **Approach changed twice mid-build, both worth knowing:** (a) `productCount` was first stored on `AdminCategory` and synced on every product create/move, which meant calling `setCategories` *inside* a `setProducts` updater — a side effect in a state updater, wrong under StrictMode. The Inventory query fetches the **entire catalog unfiltered**, so counts are now derived with `useMemo` and cannot drift; all the bookkeeping went away. (b) `slugify` was duplicated in the route and the view; it is now `slugifyCategory` in [options.ts](../src/lib/admin/options.ts), covered by tests, shared by both. **Two previously invisible couplings surfaced:** the product form now states whether the chosen jewelry type shows the product on the Jewelry page or hides it (`NON_JEWELRY_TYPE_VALUES` moved to [options.ts](../src/lib/admin/options.ts), and [queries/product.ts](../src/lib/queries/product.ts) **imports it rather than restating the pair**), and the category list shows how the published hygiene exclusion classifies each slug for returns — a new category reads **"Returns: sealed only"** until a developer adds it to the allowlist in [returns.ts](../src/lib/account/returns.ts), which is the deliberate default from 2026-08-16. **Filtering needed no work** — the `/products` chip row is built from `getHomeCategories()`, which returns every row, so a new category appears automatically; this was verified before building rather than assumed. **Homepage grid capped at 8** (two full rows of four) via a new optional `limit` on `getHomeCategories`; `/products` is deliberately left **uncapped** so a ninth category stays reachable. Ordering is alphabetical, so *which* eight is not curated — picking would need an ordering or `featured` column on `Category`, i.e. a migration. **Accessibility:** the modals initially added **9 new lint warnings** by copying the existing click-on-scrim pattern from `product-form.tsx`; replaced with a real `<button>` backdrop plus Escape-to-close, which fixes the warnings *and* makes the dialogs keyboard-dismissable — lint stayed at its pre-existing **28**. **Verification:** `npx tsc --noEmit` clean, `npm test` **27/27** (7 new, covering slug derivation, idempotency, empty-input rejection, and that `NON_JEWELRY_TYPE_VALUES` only names real enum members), `npm run lint` 28 warnings unchanged, `npm run build` clean with `/api/admin/categories` registered as `ƒ`. **Not verified in a browser** — `/admin` needs a signed-in admin session, so creating a category, seeing its chip on `/products`, and finding it in the product dropdown all rest on code review. Worth a manual pass. **Still open:** [footer.tsx](../src/components/layout/footer.tsx) hardcodes five category links, so new categories never appear there; and `products/page.tsx` hardcodes `aftercare`/`merch` as the two slugs that suppress the filter bar.
- 2026-08-03 — **Terms of Service at `/terms` + Returns Policy at `/help/returns` + Shipping Policy at `/help/shipping`, on a shared legal-page shell** — [features/terms-of-service-spec.md](features/terms-of-service-spec.md) → [src/lib/legal/types.ts](../src/lib/legal/types.ts), [src/components/legal/legal-blocks.tsx](../src/components/legal/legal-blocks.tsx), [src/components/legal/legal-page.tsx](../src/components/legal/legal-page.tsx), [src/lib/legal/terms.ts](../src/lib/legal/terms.ts), [src/lib/legal/returns-policy.ts](../src/lib/legal/returns-policy.ts), [src/lib/legal/shipping-policy.ts](../src/lib/legal/shipping-policy.ts), [src/app/terms/page.tsx](../src/app/terms/page.tsx), [src/app/help/returns/page.tsx](../src/app/help/returns/page.tsx), [src/app/help/shipping/page.tsx](../src/app/help/shipping/page.tsx), [src/lib/account/returns.ts](../src/lib/account/returns.ts), + refactors to [src/app/privacy/page.tsx](../src/app/privacy/page.tsx), [src/lib/legal/privacy.ts](../src/lib/legal/privacy.ts), [src/components/account/shared.tsx](../src/components/account/shared.tsx), [src/components/account/account-client.tsx](../src/components/account/account-client.tsx). **Fills three dead footer links in one pass** — `/terms` ([footer.tsx:86](../src/components/layout/footer.tsx#L86)) plus `/help/shipping` and `/help/returns` ([footer.tsx:20-21](../src/components/layout/footer.tsx#L20)); no footer change needed (the owner had already fixed the `href="\terms"` backslash mid-feature). **Shared shell (decision 1 at `start`):** [types.ts](../src/lib/legal/types.ts) holds `Run`/`Rich`/`Block`/`Section` plus `LegalContact` and a `LegalDocument` describing a whole document; [legal-blocks.tsx](../src/components/legal/legal-blocks.tsx) renders blocks (`p`/`list`/`sub`) and inline runs — **internal `/` hrefs render as `next/link`** for client-side nav, `http(s)` gets `target=_blank`+`rel=noopener`, `mailto:` stays a plain anchor; [legal-page.tsx](../src/components/legal/legal-page.tsx) is the entire layout (hero w/ breadcrumb → eyebrow → Italiana `h1` + serif-italic accent → "Last Updated" → intro → labelled jump-chip `nav` at `min-h-[44px]`, then numbered `01 · Kicker` sections with `scroll-mt-[120px]` + alternating `bg-canvas-elev` at a **740px measure**, closing on the pink-gradient contact card with a `<dl>` of email/website). **All four legal pages are now data-only 13-line files** — metadata + `<LegalPage doc={…} />` — so they cannot drift. **`/privacy` refactor proven text-neutral:** diffed the rendered visible text against the pre-refactor capture — identical length (6785 chars) with exactly one intended difference (`Last updated` → `Last Updated`, a fix from the prior session, not the refactor). **`/terms` ships the owner copy verbatim** — 90 spec lines checked, 87 exact; the 3 misses are the deliberate `Email:`/`Website:` → `<dl>` reformat and one tag-stripper false positive on the Governing Law sentence (raw HTML confirms `laws of <strong>Trinidad and Tobago</strong>, without regard…` renders correctly). Heading hierarchy corrected from the source's **18 `h1`s** to **1 `h1` + 18 `h2` + 0 `h3`**; "Agreement to These Terms" folded into the hero unheaded, matching how privacy's "Introduction" was handled. **Decisions (locked at `start`):** (1) **shared page shell** over per-page duplication; (2) **jump chips** for all 18 sections, matching `/privacy`; (3) intro **folded into the hero**; (4) **copy shipped verbatim**; (5) dead routes deferred — *then superseded mid-feature*, see below. **Scope expanded mid-feature at the owner's request** ("create return policy link"): rather than soften the two clauses pointing at non-existent documents, **both policies were written** and both clauses now link to them. That copy was **not owner-supplied** — it is grounded in the codebase wherever the code pins it down: **shipping rates are generated from [lib/checkout/shipping.ts](../src/lib/checkout/shipping.ts)**, the same module the checkout API charges from, so the published Pickup-free / TTPost-$25 / Courier-$40 rates and ETAs **cannot drift from what a customer pays** (verified in the rendered HTML); **returns eligibility** follows the account area's real delivered-only rule and the **reasons list is generated from `RETURN_REASONS`**; the hygiene exclusion is written to agree with the Terms clause. `RETURN_REASONS` was **moved from [components/account/shared.tsx](../src/components/account/shared.tsx) (a `"use client"` module) to [lib/account/returns.ts](../src/lib/account/returns.ts)** so a static policy page doesn't pull client components into its server graph (`account-client.tsx` import updated). **⚠ Every invented value is quarantined in a flagged `OWNER_DECISIONS` block** at the top of each policy module, each used exactly once: returns **window 7 days**, refunds **5–10 business days**, change-of-mind postage on **the customer**, **no restocking fee**; shipping **1–2 business days** processing, **12:00 pm** cut-off, **10 business days** to lost-parcel, **48 hours** to report damage. **These are defaults, not facts — they need owner sign-off before launch.** **⚠ Conflict this surfaced:** "**free shipping over $80**" is advertised in **four** places ([cart-client.tsx:282](../src/components/cart/cart-client.tsx#L282), [trust-strip.tsx:32](../src/components/cart/trust-strip.tsx#L32), [products/[slug]/page.tsx:205](../src/app/products/%5Bslug%5D/page.tsx#L205), [product-trust-badges.tsx:33](../src/components/product/product-trust-badges.tsx#L33)) but [shipping.ts](../src/lib/checkout/shipping.ts) has **flat fees with no threshold**. The Shipping Policy deliberately publishes the **flat rates** — writing the $80 threshold into policy would commit the business to a discount the code refuses to give — so **the policy is now correct and the marketing copy is wrong**; either implement the threshold or remove those four claims (left untouched: a pricing decision, and it predates this feature as the accepted `/cart`-vs-`/checkout` divergence). **Verification:** `npx tsc --noEmit` clean, `npm run lint` clean (the lone warning is the pre-existing unused `ProductReviews` import in the PDP), `npm test` **15/15**, `npm run build` clean with **`/terms`, `/help/returns`, `/help/shipping` and `/privacy` all `○` static**. Live against the production build: `/terms` 200, `/help/returns` 200, `/help/shipping` 200, `/privacy` 200, `/learn/sizing` 200, `/account` 307 (expected auth redirect); structure confirmed as terms 1/18 headings + 18 chips, returns 1/11 + 11 chips, shipping 1/10 + 10 chips, eyebrows numbered 01–18. **Not visually verified** — the Playwright MCP server never connected across either legal session, so responsive breakpoints, light theme, and the 18-chip row wrapping (the densest on the site) rest on code review and server-render checks rather than browser observation. Worth a manual pass on `/terms` at a narrow viewport. **Still open:** the four `OWNER_DECISIONS` groups above; the `$80` conflict; `/accessibility` remains the last dead footer link; the review-license clause in both Terms and Privacy is still inoperative (no `POST /api/reviews`, PDP reviews block commented out); the Terms' hygiene-based return exclusion still disagrees with `/account`'s Returns view, which accepts a request against **any** delivered order; and `NEXT_PUBLIC_SITE_URL` is still unset while all four legal pages assert **www.sherryberries.com** with a contact address that differs from the unset `CONTACT_EMAIL` and from the Resend account owner receiving all mail.
- 2026-08-01 — **Privacy Policy page at `/privacy`** — [features/privacy-policy-spec.md](features/privacy-policy-spec.md) → [src/app/privacy/page.tsx](../src/app/privacy/page.tsx), [src/lib/legal/privacy.ts](../src/lib/legal/privacy.ts). Renders the owner-supplied policy **verbatim**, filling the previously dead footer "Privacy" link ([footer.tsx:83](../src/components/layout/footer.tsx#L83)) — no footer change needed. **Content lives in a typed data module** ([privacy.ts](../src/lib/legal/privacy.ts)) per the [sizing/data.ts](../src/lib/sizing/data.ts) convention: `SECTIONS` of `{ id, title, chip, kicker, blocks[] }` where a block is `p` | `list` | `sub`, plus `LAST_UPDATED`, `SITE_URL`, `PRIVACY_EMAIL`, `INTRO` and a separate `CONTACT`. **Inline emphasis and links are modelled as a typed `Run[]`** (`string` | `{ b }` → `<strong>` | `{ href, text }` → real `<a>`, http links get `target=_blank`+`rel=noopener`) so the handful of emphasised sentences render correctly **without adding a markdown dependency** — plain strings stay plain strings for the ~90% of copy needing no markup. **Page** ([page.tsx](../src/app/privacy/page.tsx)) is a server component mirroring [/learn/sizing](../src/app/learn/sizing/page.tsx): hero (breadcrumb → pink eyebrow → Italiana `h1` "Privacy *policy*." with serif-italic second word → uppercase "Last Updated" line driven by the constant → intro → **13 jump chips**, each `min-h-[44px]`, in a labelled `<nav>`/`<ul>`), then `SECTIONS` looped into `<section>`s with `scroll-mt-[120px]`, alternating `bg-canvas-elev`, numbered eyebrows (`01 · Collection`), and a **740px measure**; Contact Us closes in the pink-gradient card matching the sizing page's CTA, with email/website as a `<dl>` of 44px-min anchors. Lists use a custom pink-dot marker; `BlockView`/`RichText` are local renderers. **Decisions (locked at `start`):** (1) route **`/privacy`** (matches the footer link) over `/legal/privacy`; (2) **jump-link chips** over a sticky sidebar index; (3) "Last Updated" from a **`LAST_UPDATED` constant** so it can't drift from the copy; (4) **ship the copy verbatim** — accuracy items left for the owner; (5) **`/terms` deferred** to a follow-up. **Heading hierarchy corrected** (the only structural deviation): the source used `#` for all 16 headings, which would have produced 16 `<h1>`s; rendered instead as **1 `h1`, 13 `h2` (12 sections + Contact), 6 `h3`** (the sub-blocks under "Information We Collect"). Two other deliberate reformats: the `**Last Updated: …**` line became the hero's styled meta line, and the `**Email:** / **Website:**` pair became a `<dl>`. **Verification:** served the production build and diffed rendered text against the spec — **108 spec lines checked, 105 matched verbatim**; the 4 reported misses were those 3 structural reformats plus 1 false positive from the tag-stripper inserting a space before a period after `<strong>WiPay</strong>` (confirmed correct in the raw HTML). Rendered structure confirmed: `GET /privacy` **200**, 1/13/6 headings, all section anchors present, 13 chips, mailto + last-updated present; `/` and `/learn/sizing` still 200. `npx tsc --noEmit` clean, `npm run lint` clean (the one warning is the pre-existing unused `ProductReviews` import in the PDP), `npm test` **15/15**, `npm run build` clean with `/privacy` as **`○` static**. **Notes:** the `viewport`-in-metadata build warning on `/privacy` is the inherited root-layout one (also fires on `/wishlist`, `/verify-email`, `/reset-password`) — not introduced here. **Not visually verified** — the Playwright MCP server never connected this session, so responsive breakpoints, light theme, and chip wrapping rest on code review + server-render checks, not browser observation. **Open for the owner (copy shipped as-is, per decision 4):** (a) **Google Analytics** is named in Third Party Services but is **not integrated** — no `gtag`/`googletagmanager`/`@next/third-parties`/measurement-ID env var; (b) cookies are described and **EEA/GDPR rights asserted with no cookie consent banner** on the site; (c) the **review-submission clause is aspirational** — the `Review` model exists and reviews render, but there is no submission flow (no `POST /api/reviews`, PDP reviews block commented out); (d) the policy asserts **www.sherryberries.com** while `NEXT_PUBLIC_SITE_URL` is unset, and its contact address **sherryvanessanichols@gmail.com** differs from the unset `CONTACT_EMAIL` and from the Resend account owner that currently receives all mail — reconcile so a privacy request actually reaches someone. **Side note:** the footer's `/terms` link had `href="\terms"` (backslash) when this feature started; the owner fixed it to `/terms` mid-feature, so it now routes correctly — but `/terms`, `/accessibility`, `/help/shipping` and `/help/returns` remain dead routes.
- 2026-06-04 — **Single-page Checkout at `/checkout` + in-page Thank-You (real orders + Resend email)** → [src/app/checkout/page.tsx](../src/app/checkout/page.tsx), [src/app/api/checkout/route.ts](../src/app/api/checkout/route.ts), [src/lib/checkout/shipping.ts](../src/lib/checkout/shipping.ts), [src/components/checkout/](../src/components/checkout/) (checkout-client, checkout-form, checkout-summary, thank-you, shared), [src/lib/email/resend.ts](../src/lib/email/resend.ts), [src/components/providers/cart-provider.tsx](../src/components/providers/cart-provider.tsx), [src/components/cart/cart-client.tsx](../src/components/cart/cart-client.tsx), [src/proxy.ts](../src/proxy.ts). Server shell `/checkout` (auth-gated like `/wishlist`/`/account`: `auth()` → `redirect("/login?callbackUrl=/checkout")`, prefills contact + default address) → client `<CheckoutClient>`: back-to-bag link + **step indicator** (Bag done → Checkout active → Confirmation upcoming) + Italiana **"Secure checkout."**; `1fr/400px` layout (stacks <940px) with a **4-section form** ([checkout-form.tsx](../src/components/checkout/checkout-form.tsx)) — Contact (first/last + email/phone), Shipping address (address/city/optional landmark), **Shipping method** (3 custom radio cards: Pickup Curepe **Free**/TTPost **$25**/Courier **$40**, icon+title+sub+price, selected = pink ring + filled dot), **Payment** (COD + Credit Card w/ WiPay tag → `grid-rows` card-fields reveal + green-lock note) — and a **sticky pink Order summary** ([checkout-summary.tsx](../src/components/checkout/checkout-summary.tsx)): 56px thumb + pink qty badge, promo row → "✦ CODE applied" chip, totals (subtotal, blush discount, shipping label + green "Free", Playfair grand total), gold→pink Place-order button + secure note. Place-order validation (first/last/email-regex/phone≥7/address/city; red field + per-field msg + focus-first + toast) → POST → swaps to the **Thank-You** state ([thank-you.tsx](../src/components/checkout/thank-you.tsx)): pop-in checkmark + radial glow, "Thank you, *{first}*!", confirmation-to-email line, pink order card (number + Processing badge; Date/Items/Ship to/Method/Payment/optional Discount/Total), pink next-steps strip ("{eta}. We'll text {phone}…" + COD-only "have payment ready on arrival"), **Track my order →** `/account?view=orders` + **Continue shopping →** `/products`. **Real, auth-gated order creation:** `POST /api/checkout` recomputes totals from the **DB cart** (never trusts client prices), re-validates the promo against `DiscountCode`, then in **one `$transaction`** creates `Order`+`OrderItems`, **atomically compare-and-decrements** the gating stock counter (`updateMany … where inventory ≥ qty`; `count===0` → `OversoldError` → roll back → **409**, so concurrent orders can't oversell — decrements variant stock if a variant was chosen, else product, mirroring the availability check), empties the cart, and bumps `DiscountCode.timesUsed`; then sends a **best-effort Resend confirmation email** ([sendOrderConfirmationEmail](../src/lib/email/resend.ts) — dark/pink template w/ line-item table + totals; order never fails on email trouble; dev-logs the send). **Decisions (locked at `start`):** (1) **real + auth-gated** (`Order.userId` is required) **+ confirmation email with items**; (2) **checkout per brief: flat T&T shipping, NO tax** — which fits the `Order` schema (cols `subtotal`/`shippingCost`/`total` only; **no tax/discount/address columns**), so discount stays *derived* and the typed shipping details are **snapshotted into `Order.notes`** (no migration); the existing `/cart` summary keeps its 8.75%-tax + free-ship-≥$80 display, so **the grand total can differ between `/cart` and `/checkout`** (accepted trade-off); (3) **inventory decremented** in the transaction. Other defaults: COD → `paymentStatus PENDING`, mock card → `PAID` (UI-only, no real WiPay); new orders `fulfillmentStatus UNFULFILLED` → render as "Processing" in `/account`; `orderNumber` = `SB-`+8 digits (matches seed); **shared [shipping.ts](../src/lib/checkout/shipping.ts)** single-sources fees/labels/ETAs across API + client. **Cart hand-off:** `/cart` "Secure checkout" now persists any applied promo to `sessionStorage["sb-promo"]` and `router.push("/checkout")` (was a mock toast); checkout re-hydrates + **re-validates** that promo via `/api/promo`; `CartProvider` gained **`clearCart`** so the navbar bag badge resets post-order. `/checkout` added to the proxy matcher (defense-in-depth alongside the page guard). **Deviations:** variant line shows `variantValue`/category, not literal "material · size · stone" (single-value variant schema; server-side order + email *do* include `material · value`); card fields not validated (mock); "Continue shopping" → `/products` (not home `#shop`); literal `checkout.html`/`.js`/`styles.css` not created (stack-translated); WhatsApp FAB still deferred. **Lint + prod build clean**; `/checkout` is `ƒ` dynamic, `/api/checkout` registered; the `/checkout` `viewport`-in-metadata warning is the inherited root-layout one (same as `/cart`,`/wishlist`). **Verification note:** **not runtime-verified in a browser** — needs a signed-in session with cart items; during `complete` the environment had **Neon Postgres + GitHub both unreachable** (network/DB outage — the homepage threw `Can't reach database server` on `getHomeCategories`, unrelated to this code). Worth a manual pass once connectivity returns: sign in as `admin@sherryberries.test`, add a product, place an order (COD + a promo like `BERRY10`), confirm Thank-You + the order appearing in `/account` → Orders + stock decrement. **PENDING PUSH:** merged to local `master` (`164a130`) but **not pushed to origin** (GitHub was unreachable at completion) — run `git push origin master` once the network is back.
- 2026-06-04 — **My Account area at `/account` (logged-in only)** → [src/app/account/page.tsx](../src/app/account/page.tsx), [src/lib/queries/account.ts](../src/lib/queries/account.ts), [src/app/api/account/profile/route.ts](../src/app/api/account/profile/route.ts), [src/app/api/account/addresses/route.ts](../src/app/api/account/addresses/route.ts), [src/app/api/account/password/route.ts](../src/app/api/account/password/route.ts), [src/components/account/](../src/components/account/) (account-client, shared, order-views, returns-view, addresses-view, profile-view, security-view). Server shell `/account` (mirrors `/wishlist`: `auth()` → `redirect("/login?callbackUrl=/account")`, then fetch) → client `<AccountClient>` orchestrating **6 sidebar views + 1 hidden order-detail view**: sticky sidebar (gradient-initials avatar, name, "★ Sweet Berry" tier, icon nav w/ live **Orders/Returns count badges**, active = pink-gradient+blush+border, red-hover Sign out → `signOut({callbackUrl:"/login"})`, horizontal-scroll on mobile); header "Hello, *first*." (Italiana + blush italic, driven by profile state); **Dashboard** (3 stat cards + recent-4 orders); **Orders** (rows w/ status badge, date+payment, ≤4 thumbnails + "+N", Playfair total, Track hidden when Cancelled); **Order detail** (back link, Order-info + Shipping cards w/ dashed rows, Items + totals incl. optional blush discount + Playfair grand total, **5-stage tracking timeline** Pending→Delivered w/ done/current[glow]/pending dots + filled bars — skipped when Cancelled, "Request a return" → Returns prefilled); **Returns** (eligible-order → product → reason → notes form w/ validation → `RT-####` prepend + toast; list w/ status badges + empty state); **Addresses** (2-col grid, default pink-tinted + "✦ Default", Set-default/Edit/Delete + dashed add tile, add/edit modal validating name+line1+city, single-default flip, delete-confirm + promote-first); **Profile** (form validating email+phone → persist + live header/sidebar update + "Member since {date}"); **Security** (change-password validation + red danger-zone delete-account confirm modal → toast → redirect home ~1.8s). Centralized **modal** (scrim+box, scale/translate, scrim-click + Escape close) + **toast**; **`?view=` deep-linking** (default Dashboard). **Data — hybrid real Prisma:** [account.ts](../src/lib/queries/account.ts) `getAccountData(userId)` serializes the signed-in user's real `User`/`Address`/`Order`+`OrderItem` and **derives the presentational gaps** — display status from `FulfillmentStatus`, 5-stage tracking index, `discount = max(0, subtotal+shipping−total)`, `returnEligible = DELIVERED`, `memberSince` from `User.createdAt` (Decimals → `Number()`; "Packed" stage has no enum member so it only reads done once shipped). **Mutations** via new auth-gated routes: `PATCH /api/account/profile` (unique-email guard), `POST/PATCH/DELETE /api/account/addresses` (single-default `$transaction`, `makeDefault` flip, promote-on-delete; maps spec fields → schema `fullName`/`region`/`postalCode`/`addressLine*`), `POST /api/account/password` (bcrypt-compare current → hash new). **Decisions / deviations:** (1) **Returns is client-state only** (sessionStorage `sb-acct-returns`, seeded with 2 demo requests from the user's real orders on first visit) — no schema model, the one mock piece, per the `start` decision; (2) cross-view actions are **React callbacks**, not the spec's vanilla `data-attribute` delegated click listener; (3) **ship-to = the account's default address for every order** — `Order` carries no address snapshot (schema gap, flagged at `start`); (4) **tier "Sweet Berry"** is presentational (no backing field); (5) **delete-account is a mock** (toast → redirect, no deletion endpoint) — matches the spec's "scheduled for deletion" wording; (6) literal `account.html`/`account.js`/`data.js`/`styles.css` **not** created (stack-translated to Next/React/TS/Tailwind, per Login/Cart/Wishlist convention); (7) **WhatsApp FAB** not added (deferred site-wide, consistent with cart/wishlist). **Lint + prod build clean**; `/account` is `ƒ` dynamic (calls `auth()` + DB), all three `/api/account/*` routes registered. The three `setState`-in-effect calls (returns hydration, `?view=` read, returns prefill) are wrapped in `queueMicrotask` per the project's React 19 no-sync-setState-in-effect convention. **Known limitation:** changing email via Profile updates the DB but the session JWT keeps the old email/name until next sign-in (no user-visible breakage — navbar shows neither). **Verification note:** interactive browser `/verify` was **not completed** — the run was interrupted and a live smoke-test against the dev server was inconclusive in this shell (port-3000 conflict with an existing dev instance + localhost/IPv4 connectivity); completion rests on clean lint + production build + code review, not runtime observation. Worth a manual pass at `/account` (sign in as `admin@sherryberries.test` — if that user has no seeded orders/addresses, the Dashboard/Orders/Addresses empty states render instead of populated data).
- 2026-06-01 — **Forgot Password (Reset via Resend email)** → [src/lib/auth/password-reset.ts](../src/lib/auth/password-reset.ts), [src/lib/email/resend.ts](../src/lib/email/resend.ts), [src/app/api/auth/forgot-password/route.ts](../src/app/api/auth/forgot-password/route.ts), [src/app/api/auth/reset-password/route.ts](../src/app/api/auth/reset-password/route.ts), [src/app/forgot-password/page.tsx](../src/app/forgot-password/page.tsx), [src/app/reset-password/page.tsx](../src/app/reset-password/page.tsx), [src/components/auth/forgot-password-form.tsx](../src/components/auth/forgot-password-form.tsx), [src/components/auth/reset-password-form.tsx](../src/components/auth/reset-password-form.tsx). The login page's pre-existing "Forgot password?" link (→ `/forgot-password`) now resolves to a real flow. **Token helper** ([password-reset.ts](../src/lib/auth/password-reset.ts)) mirrors the email-verification pattern but **reuses the `VerificationToken` table under a `reset:<email>` identifier namespace** — so **no migration** and the two flows' `deleteMany`/consume never clobber each other (32-byte hex, **1h TTL**, single-use): `createPasswordResetToken` (clears prior reset tokens, mints fresh), `findValidResetToken` (non-consuming check for page render), `consumePasswordResetToken` (re-validates → bcrypt-hashes → updates `User.password` → deletes token; clears expired tokens on the way out). **Email:** `sendPasswordResetEmail` added to [resend.ts](../src/lib/email/resend.ts) (matching dark/pink HTML template, "expires in 1 hour" copy). **`POST /api/auth/forgot-password`** is **enumeration-safe** — one generic reply (`"…check your inbox for the next step."`) for every account state — and branches on account state: **verified** account → sends the reset link; **unverified** account → sends a *verification* link instead (verify-first, reusing `createVerificationToken` + `sendVerificationEmail`), so resetting is gated on the same email-ownership proof as sign-in; **no account** → nothing. Dev console-logs the URL (`[reset-password]` / `[reset-password → verify-first]`). **`POST /api/auth/reset-password`** validates token + password (≥8, match), consumes the token, sets the new password (`400` + `reason: invalid|expired` on failure). **Pages:** `/forgot-password` (server shell + `ForgotPasswordForm` client — email input → generic confirmation explaining the verify-first path); `/reset-password` (server-validates token **non-consuming** on render → renders `ResetPasswordForm` with strength meter + confirm field + success→sign-in CTA when valid, else an invalid/expired card with a "request a new link" CTA). **Token consumed on submit, not link-click** (opening the link only validates/renders the form). Lint + prod build clean; `/reset-password` is `ƒ` dynamic (awaits `searchParams` + DB lookup), `/forgot-password` is static. **Decisions (per follow-up requests):** (1) reset **does not** touch `emailVerified` — kept verification as its own chokepoint so reset can't be a side-door past the login gate ("leave as-is" chosen); (2) reset is **gated on `emailVerified`** + unverified requesters are routed to verify-first; (3) skipped the optional polish (distinct expired-vs-invalid page state; text "Show/Hide" instead of the login page's eye-icon SVGs). **Notes:** Resend test sender only delivers to the account owner until a domain is verified — console URL is the dev path; the `/reset-password` `viewport`-in-metadata build warning is the pre-existing root-layout warning (also fires on `/verify-email`, `/wishlist`), inherited not introduced. **Low-severity edge:** a reset token manually pasted into `/verify-email` would 500 (no user matches the `reset:` identifier) — only reachable by hand-editing a URL across flows; normal emails never cross-link.
- 2026-06-01 — **Full-page Wishlist at `/wishlist` (logged-in only)** → [src/app/wishlist/page.tsx](../src/app/wishlist/page.tsx), [src/components/wishlist/](../src/components/wishlist/) (wishlist-client, wish-card, wish-recs, wish-empty), [src/app/api/wishlist/snapshot/route.ts](../src/app/api/wishlist/snapshot/route.ts), [src/proxy.ts](../src/proxy.ts), [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx), [src/components/home/wishlist-button.tsx](../src/components/home/wishlist-button.tsx), [src/components/home/bestsellers.tsx](../src/components/home/bestsellers.tsx), [src/lib/queries/home.ts](../src/lib/queries/home.ts), [src/components/product/product-wishlist-button.tsx](../src/components/product/product-wishlist-button.tsx). Server shell `/wishlist` → client `<WishlistClient>` (mirrors `/cart`): **bulk bar** ("N pieces saved" + running total of all saved prices), **toolbar** (auto-generated category filter chips, active inverts to solid `--ink`, + "Showing X of Y"), responsive 4→3→2→1 **grid** of `<WishCard>` (1.06× image zoom, full-cover PDP link, Bestseller/New tag pill, glass remove-heart, "Added X ago" badge, ratings, material/tag chips, struck-through price, pink **Add** → green "✓ In bag"/navigate-to-cart), 300ms `.removing` scale-0.92/fade with optimistic snapshot filter, **recs** rail (featured products not already wishlisted, each with a save heart), **empty state**, and header **Share** (clipboard) + **Add all to bag**. **APIs:** new `POST /api/wishlist/snapshot` enriches the user's DB wishlist (image, price, compareAtPrice, category, rating, material+tag chips, Bestseller/New pin, `addedAt` from `Wishlist.createdAt`); recs pool from `listProducts()` (featured, top 12) filtered client-side. Reuses the real `WishlistProvider`/`CartProvider` and the global navbar/footer chrome; toast + light-theme parity copied from `/cart`. **Decisions:** no `sessionStorage["sb-wish"]` / no demo-seed (spec's vanilla constructs dropped — real Prisma + providers); "Added X ago" derived from DB `createdAt` (signed-in only). **Auth-gated (per follow-up request "wishlist only for logged-in users" + "hide wishlist for non-logged-in users"):** `/wishlist` protected by the proxy matcher *and* a page-level `auth()` → `redirect("/login?callbackUrl=/wishlist")`; snapshot endpoint 401s guests; every wishlist heart (navbar desktop+mobile, homepage bestsellers cards, PDP "Save") renders only when `useSession().status === "authenticated"` (hidden during `"loading"` → server/first-client render agree, no hydration mismatch). **Bug fixed mid-feature:** the homepage bestsellers heart ([home/wishlist-button.tsx](../src/components/home/wishlist-button.tsx)) was **cosmetic-only** (local `useState`, no persistence) — the user reported toggling it didn't populate the wishlist; rewired it to `useWishlist().toggle(productId)` with `isWishlisted` state + a `busy` guard, so every heart now writes to the `Wishlist` table. `getBestsellers` now returns product `id`. Lint + prod build clean. **Notes:** `/wishlist` is `ƒ` dynamic (calls `auth()`); the pre-existing root-layout `viewport`-in-metadata warning ([layout.tsx:39](../src/app/layout.tsx#L39)) is unrelated and left as-is. Navbar "Learn"→"FAQ" relabel (href `/faq`) was a concurrent user edit included in the same commit.
- 2026-05-31 — **Products Navbar Dropdown** → [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx). The navbar "Products" link became a dropdown with three children: **Jewelry** → `/products`, **Aftercare** → `/products?category=aftercare` (same href the footer's "Aftercare Shop" uses), **Merchandise** → `/products?category=merch`. `NAV_LINKS` items gained an optional `children: NavChild[]`; the desktop link/mobile-link classNames were extracted to module consts (`desktopLinkClass`/`mobileLinkClass`) so the dropdown trigger matches the other nav items. **Desktop:** new `NavDropdown` client component — opens on hover (120ms close delay via `closeTimer` ref to avoid flicker) and on click; chevron (`ChevronDownIcon`) rotates 180° when open; closes on `Escape`, outside `mousedown`, or item select; accessible via `aria-haspopup="menu"`/`aria-expanded`/`aria-controls` + `role="menu"`/`menuitem`. Panel conditionally rendered (not just hidden) so closed items aren't tab-focusable; styled to match the glass pill (`backdrop-blur`, pink-accent shadow, `bg-[rgba(15,12,13,0.97)]` + light-theme variant). **Mobile:** the three children render inline as indented sub-links (left border, `text-ink-faint`, smaller tracking) under a "Products" header link inside the existing menu panel — no hover dependency. Lint + prod build clean. **Notes:** "Jewelry" has no dedicated category slug, so it maps to the all-products view; kept the `border-white/[0.06]` bracket-opacity form to match the file's convention despite the canonical-class lint suggestion.
- 2026-05-27 — **Email verification on registration (Resend)** — [src/lib/email/resend.ts](../src/lib/email/resend.ts), [src/lib/auth/verification.ts](../src/lib/auth/verification.ts), [src/app/api/auth/register/route.ts](../src/app/api/auth/register/route.ts), [src/app/verify-email/page.tsx](../src/app/verify-email/page.tsx), [src/app/api/auth/resend-verification/route.ts](../src/app/api/auth/resend-verification/route.ts), [src/auth.ts](../src/auth.ts), [src/app/login/page.tsx](../src/app/login/page.tsx). New customers are created **unverified** (`User.emailVerified = null`); register mints a one-time token (`VerificationToken`, identifier = email, 32-byte hex, 24h TTL, old tokens cleared) and emails a verify link via Resend (`sendVerificationEmail`, from `EMAIL_FROM` default `SherryBerries <onboarding@resend.dev>`); dev also `console.log`s the URL. `/verify-email?token=…` consumes the token (sets `emailVerified = now()`, deletes token) and renders success/expired/invalid with a resend affordance. `authorize` blocks sign-in for unverified users ([src/auth.ts:36](../src/auth.ts#L36) `if (!user.emailVerified) return null;`); register returns `{ requiresVerification: true }` and the login page shows "check your email" instead of auto-signing-in. `resend-verification` re-mints for an unverified email (enumeration-safe no-op otherwise). No migration (`VerificationToken` + `User.emailVerified` already in schema). **Notes:** Resend test sender only delivers to the account owner — console URL is the dev path until a domain is verified; seeded customers with `emailVerified = null` are now blocked from login (expected). Shipped in commit `f1e7b4b` (was left marked *In Progress* in this file; recorded here retroactively).
- 2026-05-26 — **Full-page Shopping Cart at `/cart`** — [features/cart-spec.md](features/cart-spec.md) → [src/app/cart/page.tsx](../src/app/cart/page.tsx), [src/components/cart/](../src/components/cart/) (cart-client, cart-row, order-summary, saved-for-later, cart-empty, trust-strip), [src/app/api/cart/route.ts](../src/app/api/cart/route.ts), [src/app/api/cart/snapshot/route.ts](../src/app/api/cart/snapshot/route.ts), [src/app/api/promo/route.ts](../src/app/api/promo/route.ts), [src/lib/cart/local-extras.ts](../src/lib/cart/local-extras.ts), [src/components/providers/cart-provider.tsx](../src/components/providers/cart-provider.tsx), [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx). Server shell `/cart` → client `<CartClient>` (computes totals: subtotal, promo discount honoring `percentageOff`|`amountOff`, $6/line gift wrap, free shipping at `taxable ≥ $80` else $6, 8.75% tax; owns toast + the header free-shipping progress line). Cart row: image, variant chip, stock badge (green / amber "Only N left" / red), qty stepper 1–10, price with `compareAtPrice` strikethrough + "$X each" caption when qty>1. **Remove** is a prominent 36×36 circular X button at the row's top-right (pink-gradient on hover) — initial build buried it as a tiny ghost link which the user couldn't find; moved it out + dropped the duplicate. Removal does a 280ms fade then an **optimistic `setSnapshot` filter** in the same render as clearing `removingKeys`, so the row doesn't flash back before the snapshot refetch (the bug that made remove look broken). Sticky order summary with promo input (chip when applied, Enter-to-apply), totals breakdown, Afterpay caption, mock checkout (`Redirecting…` → toast), express stubs (Shop Pay / PayPal), perks. Saved-for-later strip (localStorage `sb-saved`, "+" moves back to bag). Gift wrap toggle (localStorage `sb-cart-gift`, keyed `productId::variantId`). Empty state + T&T-localized trust strip. **APIs:** `PATCH /api/cart` (set qty 1–10), `DELETE /api/cart?productId&variantId` (remove line), `POST /api/cart/snapshot` (unified enrichment — auth reads DB cart, guest enriches body items), `POST /api/promo` (validate against real `DiscountCode`, case-insensitive, checks active/expiry/usage limit). `CartProvider` gained `updateQuantity` + `removeLine` (auth → API, guest → sessionStorage). Navbar Bag CTA is now a `<Link href="/cart">` (was a dead button). Live curl-verified: `/cart` 200, promo `BERRY10` → `{percentageOff:10}`, bad code → 404, snapshot enriches a guest line, PATCH/DELETE → 401 for guests. **Deviations from spec:** vanilla-JS/HTML translated to React/Next; hardcoded promo codes (`SWEET10`/`BERRY20`/`FRESH15`) replaced with the seeded DB codes (`BERRY10`/`AFTERCARE15`/`WELCOME20`/`GLOWUP25`/`STUDIO50`); "ships from Brooklyn" → Trinidad. **Deferred:** real checkout + Shop Pay/PayPal SDKs (mocks), saved-for-later & gift-wrap DB persistence (localStorage only — no `CartItem` schema change), WhatsApp FAB ([whatsapp-toast.md](features/whatsapp-toast.md)). **Lint note:** guest-hydration + empty-cart setStates wrapped in `queueMicrotask` to satisfy React 19's no-sync-setState-in-effect rule (recurring pattern across providers/cart components).
- 2026-05-25 — **Product Detail Page (PDP) v1 + Products listing + Sign in/out + Auth-driven providers** — [features/product-page-spec.md](features/product-page-spec.md) → [src/app/products/[slug]/{page,loading,not-found}.tsx](../src/app/products/%5Bslug%5D/page.tsx), [src/app/products/page.tsx](../src/app/products/page.tsx), [src/app/api/cart/route.ts](../src/app/api/cart/route.ts), [src/app/api/wishlist/route.ts](../src/app/api/wishlist/route.ts), [src/lib/queries/product.ts](../src/lib/queries/product.ts), [src/components/product/](../src/components/product/), [src/components/providers/cart-provider.tsx](../src/components/providers/cart-provider.tsx), [src/components/providers/wishlist-provider.tsx](../src/components/providers/wishlist-provider.tsx), [src/components/auth/auth-button.tsx](../src/components/auth/auth-button.tsx), [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx). **PDP** at `/products/[slug]`: server-component page with `generateMetadata` (seoTitle/Description with name/shortDescription fallbacks + first image as og:image), breadcrumb (Home / Category / JewelryType / Name), `1.15fr / 1fr` hero grid (stacks <900px). Gallery: thumbnail strip (vertical desktop / horizontal scroll mobile), active glow, mouse-tracked hover zoom (scale 1.6×, `transform-origin` from cursor), Bestseller pill when tagged. Buy-box: variant chips with disabled+line-through when `inventory <= 0` and inverted-active state; qty stepper 1–10; live `final = base + variant.additionalPrice` price; in-stock / low-stock / out-of-stock pill; toast on add; mobile sticky bar via `IntersectionObserver` on the main CTA, only renders <880px. Accordion (5 sections, first open by default, rotating + icon, sliding `grid-template-rows` animation). Reviews: average + 5-row breakdown bars + up to 6 verified cards. Related products: 4-up grid keyed off same category OR jewelryType, featured-first. **APIs:** `POST/GET /api/cart` (auth-gated; merges duplicate productId+variantId, caps qty at 10) and `POST/GET /api/wishlist` (toggle via `userId_productId` compound key). **Providers** (`CartProvider`, `WishlistProvider`) mounted in [layout.tsx](../src/app/layout.tsx) inside `AuthSessionProvider`: auth path fetches API, guest path persists to sessionStorage (cart) / localStorage (wishlist) — different durability matches spec intent (carts ephemeral, wishlists durable). Guest hydration deferred to `queueMicrotask` to satisfy React 19's `no-sync-setState-in-effect` lint rule. Navbar dropped `wishlistCount`/`cartCount` props and now reads `useCart()` + `useWishlist()`; live curl-verified that adding 2× of a variant returns `{count:2}` and the badge updates. **/products listing** (added beyond original scope): server-rendered grid with `?category=<slug>` filtering, category chip row, dynamic `generateMetadata`, featured-first ordering, Featured/Sold-out pills, empty-state card. **Routing migration:** flat-swapped every `/shop` → `/products` (navbar label "Shop" → "Products", hero CTAs, categories cards, bestsellers cards, footer links, PDP breadcrumb), then re-pointed category links to `?category=<slug>` query strings to avoid collision with the PDP route. **AuthButton** ([src/components/auth/auth-button.tsx](../src/components/auth/auth-button.tsx)) added to the navbar between Wishlist and Bag: pill style; loading-state placeholder reserves width to prevent navbar jump on hydration; authenticated → `signOut({ callbackUrl: "/" })`, unauthenticated → `<Link href="/login">` so it works without JS and honors `pages.signIn`. **Deferred:** cart drawer UI ([cart-drawer-spec.md](features/cart-drawer-spec.md)), review submission UI + `POST /api/reviews`, `ReviewImage` schema migration, WhatsApp FAB. **Caveats / known gotchas:** (1) Footer's `SHOP` array has pre-existing wrong category slugs (`tragus` should be `cartilage-jewelry`, `septum` should be `septum-jewelry`, `waistbeads` has no matching category) — links now render the empty-state card instead of 404-ing. (2) `notFound()` returns 200 in the dev server but proper 404 in prod (Next.js dev quirk). (3) After sign-out, providers don't auto-rehydrate to the guest path until next mount — flash-free for now but worth a watch.
- 2026-05-24 — **Auth Phase 2 (Credentials + register + login-page wiring)** — [features/auth-phase-2-spec.md](features/auth-phase-2-spec.md) → [src/auth.config.ts](../src/auth.config.ts), [src/auth.ts](../src/auth.ts), [src/app/api/auth/register/route.ts](../src/app/api/auth/register/route.ts), [src/app/login/page.tsx](../src/app/login/page.tsx), [.env](../.env), [.env.example](../.env.example). Split-config pattern keeps `auth.config.ts` edge-safe with a `Credentials` placeholder (`authorize: async () => null`); real validation in `auth.ts` looks up the user by lowercase email and runs `bcrypt.compare` against `User.password`, returning `{ id, email, name, image }` on success. `POST /api/auth/register` (validated end-to-end via curl: 201 happy path, 409 duplicate email, 400 on mismatched / short password / invalid email) accepts `{ name, email, password, confirmPassword }`, hashes with bcrypt, splits `name` into `firstName`/`lastName`, creates a `CUSTOMER` user. Phase 1's GitHub provider removed entirely (project is credentials-only); cleaned `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` from `.env` + `.env.example`. `pages.signIn` set to `/login` so the proxy gate redirects to the custom page instead of NextAuth's default. **Login page wired to the real backend** ([src/app/login/page.tsx](../src/app/login/page.tsx)): login form calls `signIn("credentials", { redirect: false })` and honors `?callbackUrl=`; signup form POSTs to `/api/auth/register` then auto-signIns on 201, falling back to "please sign in" if auto-signIn fails; surfaces "Wrong email or password" on bad creds; removed the visual-only Google/GitHub/Apple social row + the dead icon components + `socialBtnClass`/`handleSocial`. Live curl verification: credentials sign-in returned 302 + `authjs.session-token` cookie, `GET /api/auth/session` resolved with the admin user including `user.id` threading through. **Decisions / gotchas:** (1) Spec said "bcryptjs (already installed)" but `bcrypt` is what's actually installed (used by the seed) — used `bcrypt` so seeded `password123` hashes verify directly; both libs produce compatible `$2b$` hashes if we ever swap. (2) `Session` table stays empty by design — `strategy: "jwt"` keeps the session in the signed/encrypted cookie, not the DB. (3) Adding/removing `proxy.ts` requires a dev-server restart — Next.js detects proxy files at boot, not via HMR.
- 2026-05-24 — **Auth Phase 1 (NextAuth v5 baseline)** — [features/auth-phase-1-spec.md](features/auth-phase-1-spec.md) → [src/auth.config.ts](../src/auth.config.ts), [src/auth.ts](../src/auth.ts), [src/app/api/auth/[...nextauth]/route.ts](../src/app/api/auth/%5B...nextauth%5D/route.ts), [src/proxy.ts](../src/proxy.ts), [src/types/next-auth.d.ts](../src/types/next-auth.d.ts). Split config pattern: `auth.config.ts` carries providers (GitHub) + `authorized` callback (edge-safe, no adapter); `auth.ts` spreads it and adds `PrismaAdapter(prisma)`, `session: { strategy: "jwt" }`, plus `jwt`/`session` callbacks to thread `user.id` into the session. Proxy uses the wrapping pattern `export const proxy = auth(() => {})` with matcher `["/account/:path*", "/admin/:path*"]` — placeholder segments since no protected routes exist yet. NextAuth's default sign-in page used for Phase 1 (no `pages.signIn` override). `AUTH_SECRET` generated and added to `.env`; `.env.example` documents `AUTH_SECRET` / `AUTH_GITHUB_ID` / `AUTH_GITHUB_SECRET`.
- 2026-05-24 — **Full Prisma Seed** — [features/seed-spec.md](features/seed-spec.md) → [prisma/seed.ts](../prisma/seed.ts). Single-file seed covering all 16 models in dependency order. Dev login: `admin@sherryberries.test` / `password123`. New deps: `@faker-js/faker`, `bcrypt`, `@types/bcrypt`.
- 2026-05-17 — **Floating Pill Navbar** — [features/navbar-spec.md](features/navbar-spec.md) → [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx), styles in [src/app/globals.css](../src/app/globals.css), mounted in [src/app/layout.tsx](../src/app/layout.tsx).
- 2026-05-17 — **Hero Section** — [features/hero-section-spec.md](features/hero-section-spec.md) → [src/components/home/hero.tsx](../src/components/home/hero.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-17 — **Categories Grid** — [features/categories-grid-spec.md](features/categories-grid-spec.md) → [src/components/home/categories.tsx](../src/components/home/categories.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-17 — **Bestsellers Product Grid** — [features/bestsellers-grid-spec.md](features/bestsellers-grid-spec.md) → [src/components/home/bestsellers.tsx](../src/components/home/bestsellers.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Brand Story** — [features/brandstory-spec.md](features/brandstory-spec.md) → [src/components/home/brand-story.tsx](../src/components/home/brand-story.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Learn / FAQ Accordion** — [features/faq-spec.md](features/faq-spec.md) → [src/components/home/faq.tsx](../src/components/home/faq.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Berry List Newsletter** — [features/newsletter-spec.md](features/newsletter-spec.md) → [src/components/home/newsletter.tsx](../src/components/home/newsletter.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Footer** — [features/footer.md](features/footer.md) → [src/components/layout/footer.tsx](../src/components/layout/footer.tsx), styles in [src/app/globals.css](../src/app/globals.css), mounted in [src/app/layout.tsx](../src/app/layout.tsx).
- 2026-05-19 — **Prisma 7 + Neon PostgreSQL** — [database-spec.md](database-spec.md), [backend-architecture.md](backend-architecture.md) → [prisma/schema.prisma](../prisma/schema.prisma), [prisma.config.ts](../prisma.config.ts), [src/lib/db/index.ts](../src/lib/db/index.ts), [prisma/seed.ts](../prisma/seed.ts).
- 2026-05-19 — **Login Page** — [features/login-page-spec.md](features/login-page-spec.md) → [src/app/login/page.tsx](../src/app/login/page.tsx).
- 2026-05-20 — **Tailwind v4 migration (cross-cutting)** — All 9 components refactored from BEM-style custom CSS to Tailwind v4 utility classes inline. [src/app/globals.css](../src/app/globals.css) shrunk **2,118 → 106 lines**.
- 2026-06-09 — **Admin Control Room at `/admin` (real-data dashboard + product CRUD, admin-only)** → [src/app/admin/page.tsx](../src/app/admin/page.tsx), [src/lib/queries/admin.ts](../src/lib/queries/admin.ts), [src/lib/admin/](../src/lib/admin/) (guard, status, options), [src/app/api/admin/](../src/app/api/admin/) (orders, inventory, products), [src/components/admin/](../src/components/admin/) (admin-client, shared, charts, overview-view, orders-view, order-detail-view, inventory-view, analytics-view, product-form), [src/components/layout/site-chrome.tsx](../src/components/layout/site-chrome.tsx), [src/app/layout.tsx](../src/app/layout.tsx). Single-page admin "Control Room" — **stack-translated** from the brief's static `admin.html`/`admin.css`/`admin-data.js` into a Next App-Router page reusing the existing `globals.css` tokens (the brief's `.nav`/`.btn`/`.status`/`.modal`/`.toast` → inline-Tailwind equivalents mirroring `account/shared.tsx`). **Chrome:** floating pill nav (logo, **light/dark toggle persisted to `localStorage["sb-theme"]`** — net-new; the site toggle is session-only — applied to `documentElement.dataset.theme`, "SA" avatar, View Store); the global store navbar/footer are **hidden on `/admin`** via a `usePathname` gate ([site-chrome.tsx](../src/components/layout/site-chrome.tsx)). **Layout:** sticky ≈250px sidebar + content, stacks <940px, "Admin / Control Room" brand block, **live sidebar badges** (orders-needing-action + low-stock, recomputed from client state so they update on edits); **client-side view router** (conditional `.adm-view` panels, no reloads) across **Overview / Orders / Inventory / Analytics + a hidden order-detail view**. **Auth:** page-level `requireAdmin()` ([guard.ts](../src/lib/admin/guard.ts): `auth()` + DB role lookup — JWT/session shape untouched) gates the page (guests → `/login?callbackUrl=/admin`, signed-in non-admins → `/`) and is reused by every admin API route (403 otherwise); proxy matcher already had `/admin/:path*` (login-level defense-in-depth). **Real Prisma data** ([admin.ts](../src/lib/queries/admin.ts) `getAdminData()`): all-time Revenue/Orders/AOV KPIs + MoM deltas, **trailing-12-month MONTHLY** revenue+orders trend, Sales-by-Category, Top-Products-by-Revenue, full inventory (sku/price/compareAt/stock/`lowStockThreshold` as "reorder"/sold-30d/status), Units Sold, Product Revenue, est. Inventory-Value-at-cost, Active SKUs. **Charts are dependency-free inline SVG** ([charts.tsx](../src/components/admin/charts.tsx)): interactive area chart (gradient fill + line draw-in + hover bands → floating tooltip), `stroke-dasharray` acquisition donut + legend, animated category bars, inline share bars. **Orders view:** status filter tabs w/ live counts + search (#/customer/email) + table (avatar+email, stacked thumbs, channel, status badge, total) → **order detail** (line items, customer card, status `<select>` → `PATCH /api/admin/orders` persists + re-renders). **Inventory view:** out/low/healthy alert tiles, All vs "Needs reorder" tabs + search, inline price input + stock stepper with **dirty-row highlighting + sticky Save/Discard bar** (`PATCH /api/admin/inventory`, batch `$transaction`, live status-pill recompute, confirm-discard modal), **plus full add/edit product CRUD** in a modal ([product-form.tsx](../src/components/admin/product-form.tsx)) → `POST`/`PATCH /api/admin/products` covering all core `Product` fields (name, SKU, short/long description, price, compare-at, stock, reorder, category, jewelry type, material, featured/active, one image URL); slug auto-generated + unique on create, left stable on update; SKU-clash 409; image = single position-0 `ProductImage`, non-destructive on empty. **Analytics view:** 4 KPI cards + reused trend chart + donut + Top-Products table w/ share bars. **Decisions / deviations (user-confirmed at `start` + during the increment):** (1) **stack-translated to `/admin`** (not literal static files); (2) **real Prisma + admin auth** (the brief's `window.SB_ADMIN` mock + `sessionStorage` persistence dropped — edits persist to the **DB**); (3) **12-month monthly trend** replaces the brief's 14-day daily chart (seed orders span a year, only ~1 in the last 14 days) + KPIs are all-time + MoM; (4) **"Packed" status tab dropped** (no `FulfillmentStatus` enum member, no migration), **Refunded** derived from `paymentStatus`; (5) **est.-labeled** Conversion %, acquisition channels, and inventory-value-at-cost (`price × 0.45` — no `cost` column), and the order "channel" column (derived from `paymentMethod`) — no real source exists for these; (6) admin product thumbnails use a plain-`<img>` `ProductThumb` (not `next/image`) so admin-entered image hosts don't trip `next.config` `remotePatterns`. **Review cleanup:** removed dead `AdminData.badges`/`inventoryTiles` fields + a wasted `order.count()` query (sidebar badges + tiles are computed live client-side). **Verified (dev server, real DB, all mutations reverted/cleaned up):** guest `/admin` → 307 `/login`; signed-in customer → 307 `/` + admin API 403; admin → 200 with real data; order status UNFULFILLED→Shipped persists + reverts; inventory stock 28→3 → "Low stock" pill + revert; product create → 200 (image preserved), update → 200 (status recomputed, compare-at cleared), missing-name → 400, duplicate-SKU → 409, guest POST → 403. **Lint + production build clean** (`/admin` is `ƒ` dynamic; `/api/admin/{orders,inventory,products}` registered; the `/admin` `viewport`-in-metadata warning is the inherited root-layout one). **Notes:** a refunded order can't be "un-refunded" via the status `<select>` (refunds are terminal); brief theme flash possible on `/admin` if `sb-theme=light` (SSR renders dark, mount re-applies — consistent with the site's existing toggle). **Build gotcha:** running `next build` while a `next dev` server is live corrupts `.next/dev/types/validator.ts` mid-write (false "Expression expected" type error) — stop the dev server / clear `.next` for a clean build (a dev server was stopped during `review`; run `npm run dev` to restart it).
- 2026-06-09 — **Rate limiting on auth endpoints (Upstash sliding window)** — [features/rate-limiting-spec.md](features/rate-limiting-spec.md) → [src/lib/rate-limit.ts](../src/lib/rate-limit.ts), [src/auth.ts](../src/auth.ts), [src/app/api/auth/register/route.ts](../src/app/api/auth/register/route.ts), [src/app/api/auth/forgot-password/route.ts](../src/app/api/auth/forgot-password/route.ts), [src/app/api/auth/reset-password/route.ts](../src/app/api/auth/reset-password/route.ts), [src/app/api/auth/resend-verification/route.ts](../src/app/api/auth/resend-verification/route.ts), [src/app/login/page.tsx](../src/app/login/page.tsx), [src/components/auth/forgot-password-form.tsx](../src/components/auth/forgot-password-form.tsx), [src/components/auth/resend-verification.tsx](../src/components/auth/resend-verification.tsx), [.env.example](../.env.example). Brute-force / credential-stuffing / email-abuse guard across all 5 auth endpoints. **New util** [rate-limit.ts](../src/lib/rate-limit.ts): a shared `@upstash/redis` client (null when creds absent) + one `Ratelimit.slidingWindow` limiter per endpoint (`authLimiters.{login 5/15m, register 3/1h, forgotPassword 3/1h, resetPassword 5/15m, resendVerification 3/15m}`); `checkRateLimit(limiter, id)` returns `{ success, remaining, reset }` and **fails open** (allows the request) when the limiter is disabled *or* Redis throws, so an Upstash outage never locks anyone out of auth; `getClientIp(req)` (`x-forwarded-for` → `x-real-ip` → `127.0.0.1`); `tooManyRequests(reset)` → `429` + `{ error: "Too many attempts. Please try again in X minutes." }` + `Retry-After`. **Wiring:** the 4 real route handlers call the limiter at the top of `POST` and early-return `tooManyRequests` (register/forgot/reset keyed by IP; resend keyed by `IP:email`, after the email is parsed). **Login** is the tricky one — the form calls `signIn("credentials")` which hits NextAuth's internal `/api/auth/callback/credentials`, not a handler we own — so limiting is enforced **inside `authorize(credentials, request)`** in [auth.ts](../src/auth.ts) (the real credential-verification chokepoint, unbypassable), keyed `IP:email`; over-limit **throws `class RateLimitSignin extends CredentialsSignin { code = "rate_limited" }`**, and that `code` surfaces to the client via `signIn`'s `SignInResponse.code`, which the login form ([login/page.tsx](../src/app/login/page.tsx)) maps to a distinct "Too many sign-in attempts" toast (vs the generic wrong-password toast). **Frontend:** [forgot-password-form](../src/components/auth/forgot-password-form.tsx) + [resend-verification](../src/components/auth/resend-verification.tsx) now read the response *only* to surface a `429` inline (red message) — otherwise still ignored to stay **enumeration-safe** (the limit is IP-keyed, so showing it leaks nothing); [reset-password-form](../src/components/auth/reset-password-form.tsx) already displayed `body.error`, so a `429` shows there with no change. New deps `@upstash/ratelimit` + `@upstash/redis`; `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` documented in [.env.example](../.env.example) (live creds already in `.env`). **Decisions (locked at `start`):** (1) **login enforced in `authorize()`** rather than a bypassable pre-flight endpoint — secure, but the toast is a friendly generic message (no precise minutes / no literal `429`/`Retry-After`, since NextAuth's error channel only carries the `code`); (2) user **supplied live Upstash creds**, so limiting was verified against real Redis. **Edge-runtime safety:** [proxy.ts](../src/proxy.ts) imports the edge-safe `auth.config.ts`, **not** `auth.ts`, so `auth.ts`'s new `@upstash/*` + `next/server` imports stay in the Node runtime only (no edge-bundling issue; prod build confirms). **Verified live against real Upstash** (synthetic TEST-NET IPs + junk/wrong creds → no users/sessions created, no real keys polluted): register → 3 allowed then `429` + `Retry-After`; login callback → attempts 1–5 `error=CredentialsSignin&code=credentials`, 6th → `code=rate_limited`. **Notes:** (1) the login limiter counts *successful* sign-ins too (sliding window counts every `authorize` call) — 5 per `IP:email` per 15 min is well above normal use, and JWT refresh doesn't re-invoke `authorize`, so impact is negligible; (2) the sliding-window `Retry-After` can be **shorter** than the nominal window (it's the time to the current bucket boundary, e.g. "8 minutes" for the 1-h register window) — truthful (it *is* safe to retry then), not a bug; (3) IP-keyed endpoints share a limit across users behind the same NAT/carrier IP — the inherent IP-limiting tradeoff, fine at this volume. **Lint + production build clean**; all 4 auth routes registered `ƒ` dynamic; only the inherited root-layout `viewport`-in-metadata warnings (`/verify-email`, `/wishlist`).
- 2026-06-21 — **WiPay card payments at `/checkout` (hosted-page web-redirect, replacing the mock card path)** → [src/lib/checkout/wipay.ts](../src/lib/checkout/wipay.ts), [src/app/api/checkout/route.ts](../src/app/api/checkout/route.ts), [src/app/api/checkout/wipay/return/route.ts](../src/app/api/checkout/wipay/return/route.ts), [src/app/checkout/confirmation/page.tsx](../src/app/checkout/confirmation/page.tsx), [src/lib/checkout/order-view.ts](../src/lib/checkout/order-view.ts), [src/components/checkout/checkout-client.tsx](../src/components/checkout/checkout-client.tsx), [src/components/checkout/checkout-form.tsx](../src/components/checkout/checkout-form.tsx), [src/components/cart/cart-client.tsx](../src/components/cart/cart-client.tsx), [.env.example](../.env.example), [CLAUDE.md](../CLAUDE.md). Integrated the **WiPay Plugins Payment Request API v1.0.8** hosted-page web-redirect flow, replacing the UI-only mock card charge. **Config** ([wipay.ts](../src/lib/checkout/wipay.ts) `getWipayConfig`): env-driven — `WIPAY_ENVIRONMENT` (`live`/`sandbox`, default sandbox) is the prod/test switch; sandbox forces the TEST creds (`1234567890`/key `123`) so it works zero-config, **live requires** `WIPAY_ACCOUNT_NUMBER`+`WIPAY_API_KEY` (throws otherwise); `WIPAY_COUNTRY_CODE`/`CURRENCY`/`FEE_STRUCTURE`/`ORIGIN` overrides (defaults TT/TTD/merchant_absorb/SherryBerries); endpoint derived per country. API Key stays server-only. **Request leg** ([route.ts](../src/app/api/checkout/route.ts)): for `card` orders, `requestHostedPage` POSTs the request (`Accept: application/json`) and gets the hosted-page URL **before** committing the order (gateway failure → error response, nothing persisted/decremented); the order is then created `paymentStatus PENDING` (was mock-`PAID`) and the API returns `{ redirect }` so the client `window.location.href`-es the payor to WiPay. COD path unchanged. **Response leg** ([wipay/return/route.ts](../src/app/api/checkout/wipay/return/route.ts)): WiPay web-redirects (GET) to `/api/checkout/wipay/return`; resolves the order by `order_id` (= our `orderNumber`); on **`status=success` verifies the hash** `md5(transaction_id + original_total + apiKey)` and only then **claims `PENDING→PAID` via `updateMany(where PENDING)`** (so concurrent/replayed callbacks email **exactly once**) + sends the Resend confirmation, redirecting to `/checkout/confirmation`. Success-with-bad-hash → left PENDING for manual review (`/checkout?wipay=error`). **Failure leg** cancels (`FAILED`/`CANCELLED`), restocks inventory, rebuilds the cart, releases the promo redemption — but **gated on the order owner's session** (`auth()` + `session.user.id === order.userId`) because WiPay does **not** sign failure callbacks (spec p.34: hash is success-only), so a forged or cross-user `?status=failed` can't grief a victim's PENDING order (relies on the `SameSite=Lax` session cookie riding WiPay's top-level GET redirect). **Confirmation page** ([confirmation/page.tsx](../src/app/checkout/confirmation/page.tsx)): owner-gated, PAID-only, renders the shared `<ThankYou>`. **Shared derivation** ([order-view.ts](../src/lib/checkout/order-view.ts) `buildPlacedOrder`): reverses the `Order.notes` snapshot into the `PlacedOrder` shape so the return handler (email) + confirmation page (Thank-You) share one path. **Frontend:** checkout form drops the on-site card inputs (PCI) for a "you'll enter your card on WiPay's secure page" notice; [checkout-client](../src/components/checkout/checkout-client.tsx) handles the `{redirect}` response + surfaces `wipay=error|pending` toasts; [cart-client](../src/components/cart/cart-client.tsx) shows a `wipay=failed` "bag saved, try again" toast. **Decisions:** (1) **both COD and card start PENDING**; card → PAID only on the verified callback; (2) `fee_structure=merchant_absorb` default (final total == sent total, so the hash uses `order.total`); (3) failure cleanup gated on owner session rather than a hash, since failures are unsigned. **`.gitignore` now ignores `/docs`** (the spec PDF). **Lint clean. Runtime-verified** (`/verify`, dev server + real DB, throwaway fixtures cleaned up): forged **unauthenticated** `status=failed` → 307 `/cart?wipay=failed`, order untouched (PENDING, no restock); authenticated **cross-user** failure → also blocked; authenticated **owner** failure → order FAILED/CANCELLED + inventory restocked; valid-hash `success` → PAID + redirect to confirmation, **replay → no second email** (claim guard held); bad-hash `success` → stays PENDING. **Known follow-ups (out of scope):** stale `PENDING` card orders with no trusted resolution (payor abandons WiPay, bad-hash success, or a failure without the owner's session) keep inventory reserved — needs an out-of-band reconciliation/expiry sweep (pre-existing gap, now also covers the session-less failure case); the optional `data` request param is still the PDF placeholder `{"a":"b"}`; **set the real production `WIPAY_API_KEY`** before going live (`.env` currently holds the sandbox key `123`).
- 2026-07-08 — **Homepage Testimonials Slider** — [features/PRD-Testimonials-Slider.md](features/PRD-Testimonials-Slider.md) → [src/lib/home/testimonials.ts](../src/lib/home/testimonials.ts), [src/components/home/testimonials.tsx](../src/components/home/testimonials.tsx), [src/app/page.tsx](../src/app/page.tsx). A client-side **Reviews carousel** on the homepage, mounted **after `<Faq />`, before `<Newsletter />`** (the PRD's "between Berry Elixir and Community" mapped to the real section order, which has neither). **Data ([testimonials.ts](../src/lib/home/testimonials.ts))**: a typed static `TESTIMONIALS` array (6 entries: `name`, `loc`, `rating`, `date`, optional `img`, `quote`) — the single editable source. **Decision (locked at `start`):** static real-customer reviews, **not** DB-backed — the project has a `Review` model but **no review-collection flow** (no `POST /api/reviews`/form), so a DB slider would be **empty at launch**; static also supplies `loc`/avatar the `Review` schema lacks. Copy is currently **plausible placeholders to be swapped for real quotes**. **Component ([testimonials.tsx](../src/components/home/testimonials.tsx))**: `"use client"` slider — responsive **3/2/1 cards per page** (`≤640`→1, `≤1024`→2, else 3) via a `ResizeObserver` + window-resize handler that also re-derives `perPage`; page count + current page clamp on layout change (`queueMicrotask` per the React-19 no-sync-setState-in-effect convention). Track is a flex row translated in **pixels** (`page * (viewportWidth + gap)`), `~550ms` eased transition. **Nav:** prev/next arrows (disabled at the ends; a duplicate set shown below the dots on mobile where the header pair is hidden), clickable pagination **dots** that track the page (`aria-current`), pointer **swipe** (40px threshold), and **6s autoplay** that loops to the first page and pauses on hover. **Card:** gold `★` rating, serif quote, `loc · date`, pink **Verified buyer** badge, and an **avatar with initials fallback** (`Avatar` uses a plain `<img>`+`onError`→initials so arbitrary hosts don't need `next.config` `remotePatterns` — mirrors the admin `ProductThumb` call — and testimonials with no `img` render initials directly). Reuses theme tokens (`bg-card`/`pink`/`gold`/`blush`/`line`) + `light:` variants; card hover = border+shadow only (no translate, since cards live in a transformed track). **Out of scope (v1, per PRD):** review submission, aggregate-rating panel, per-card product attribution, filtering/listing. **Stack translation:** the PRD's vanilla `app.js`/`styles.css`/`data.js` (`SECTION 6.5`, cache-busted query strings) became a single Next/React/TS + Tailwind component (asset hashing is Next's job). **Lint clean** for the feature files. **Runtime-verified** (`/verify` — Playwright + chromium against the dev server, screenshots): section renders after FAQ / before Newsletter; SSR HTML contains it (no-JS resting state); desktop 3 cards/2 dots, tablet(900) 2/3, mobile(500) 1/6 + mobile arrows appear; prev disabled at page 0; **next arrow**, **dot jump**, **swipe left/right**, and **6s autoplay** all change the page; avatar initials render (AM/RC/KB/SJ/ML/TP). **Bug found + fixed during verify:** the count line rendered "6verified" (JSX/Turbopack collapsed the space around `{TESTIMONIALS.length}`) → fixed with an explicit `{" "}`, re-verified as "6 verified". **Bundled per user request (unrelated in-progress edits present in the working tree at `complete`):** [navbar.tsx](../src/components/layout/navbar.tsx) — the "Products" dropdown is commented out and `NAV_LINKS` flattened (Jewelry/Aftercare/Merchandise/Bestsellers as top-level, Our Story/FAQ/Community removed); and [products/[slug]/page.tsx](../src/app/products/%5Bslug%5D/page.tsx) — the PDP `<ProductReviews>` block is commented out (leaving an **unused-import lint warning**, `'ProductReviews' is defined but never used`). These are **not** part of the slider and look unfinished; revisit them (the navbar loses its dropdown; the PDP loses its reviews section).
- 2026-07-19 — **Site Search (command-palette overlay)** — [features/site-search.md](features/site-search.md) → [src/lib/queries/search.ts](../src/lib/queries/search.ts), [src/lib/search/match.ts](../src/lib/search/match.ts), [src/lib/search/popular.ts](../src/lib/search/popular.ts), [src/app/api/search/route.ts](../src/app/api/search/route.ts), [src/components/search/search-provider.tsx](../src/components/search/search-provider.tsx), [src/components/search/search-overlay.tsx](../src/components/search/search-overlay.tsx), [src/app/layout.tsx](../src/app/layout.tsx), [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx), [src/app/globals.css](../src/app/globals.css). Activated the previously-inert header search icon into a **command-palette overlay** that searches the real catalog client-side, available on every page with the nav. **Data:** [search.ts](../src/lib/queries/search.ts) `getSearchIndex()` returns a slim snapshot of **all active products** (id/slug/name/category/material/tags/image/price/compareAt) + categories (name/slug/description), exposed at **`GET /api/search`** and **lazy-loaded once** on first open (zero weight on normal page loads). Indexes the whole catalog (incl. aftercare/merch) so search finds anything — unlike the browse pages, which the recent catalog-separation work scoped to jewelry. **Matching** ([match.ts](../src/lib/search/match.ts), pure + client-safe — `import type` from the `server-only` query is erased): case-insensitive substring, ranked **name (3) > category (2) > material/tag (1)**, name-tiebreak; categories match on name or description. **[SearchProvider](../src/components/search/search-provider.tsx)** (context + global `keydown` listener + lazy fetch w/ retry) mounted in [layout.tsx](../src/app/layout.tsx) around the site chrome, **disabled on `/admin`**; opens via the navbar's desktop **+** mobile Search buttons (`useSearch().open()`) and the global **`/`** / **`Cmd/Ctrl-K`** shortcut (`/` ignored while typing in a field). **[SearchOverlay](../src/components/search/search-overlay.tsx)** (`role="dialog"` `aria-modal`): magnifier input (`role="combobox"` + `aria-controls`/`aria-activedescendant`), **~120ms debounce**, grouped **Products → Categories → Suggestions**, product rows (44px `next/image` thumb + highlighted name + category + price/struck-was), category rows (highlighted name + description), products **capped at 6** with a "Showing 6 of N — keep typing" note (the PRD's reserved "see all" affordance; no `/search` page in v1), live result count. **Keyboard:** ↑/↓ cycle the active option (auto-highlights first result), Enter opens it (records the query to recent), Esc/scrim close, **Tab focus-trap**, body scroll-locked; result-nav keys are gated to the input so Enter on a focused chip isn't hijacked (**fixed at `review`**). **Idle state:** Recent (localStorage `sb-recent-searches`, max 6, **Clear** button) + curated **Popular searches** chips ([popular.ts](../src/lib/search/popular.ts), typed static à la `testimonials.ts`) + a few **Trending** products. **Zero-result:** ✦ + friendly message + suggested-category chips — never blank. Selecting a product → `/products/<slug>`, a category → `/products?category=<slug>`. `fadeIn`/`searchPanelIn` keyframes added to [globals.css](../src/app/globals.css) for the launch animation; `light:` theme variants throughout. **Decisions (locked at `start`):** stack-translated to Next/React/TS (no vanilla `app.js`/`data.js`); index = **all active** products; **lazy `/api/search`** fetch on first open; matching in a **pure module**; client-side v1, **no new dependencies**. **Review fixes:** Enter-on-chip hijack (gated result-nav to the input) + a contradictory `uppercase`/`normal-case` class on the count. **Runtime-verified** (`/verify` — Playwright vs. dev server): icon click + `/` + `Ctrl-K` open with input focused (home & PDP); "septum" → 9 products (6 shown + "Showing 6 of 9") + highlighted `Septum` category + Suggestions, first auto-selected; ArrowDown moves `aria-activedescendant` `p-0→p-1`; Enter → top result's PDP (`pearl` → `/products/pearl-drop-veil-clicker-23`); recent persists w/ Clear; zero-result shows category chips; Esc closes + restores scroll. **Typecheck + ESLint clean** on all feature files (only the pre-existing inherited root-layout `viewport`-in-metadata warning + the project's bracket-class style warnings remain). **Notes / follow-ups:** idle **Trending** rows are mouse-clickable but not keyboard-tabbable (results + chips are; PRD keyboard nav targets results); **light theme** relies on `light:` classes (dark runtime-verified, light not toggle-tested this pass); `POPULAR_SEARCHES` terms are editable placeholders. **Out of scope (v2):** dedicated `/search` page w/ filters/sort, fuzzy/synonyms/typo tolerance, ML ranking, voice/image, search analytics.
- 2026-07-21 — **Jewelry Sizing Guide Page (`/learn/sizing`)** — [features/sizing-guide-spec.md](features/sizing-guide-spec.md) → [src/app/learn/sizing/page.tsx](../src/app/learn/sizing/page.tsx), [src/lib/sizing/data.ts](../src/lib/sizing/data.ts), [src/components/sizing/sizing-faq.tsx](../src/components/sizing/sizing-faq.tsx), [src/components/sizing/reveal.tsx](../src/components/sizing/reveal.tsx), [src/app/globals.css](../src/app/globals.css), [src/components/layout/footer.tsx](../src/components/layout/footer.tsx). A **static, data-driven** sizing guide filling the footer's previously-dead "Sizing Guide" link (the route already pointed at `/learn/sizing`, so no footer-link change needed). **Stack-translated** (per every prior feature) from the brief's vanilla `sizing.html`/`.css`/`.js` into a Next App-Router page reusing `globals.css` theme tokens + the already-global navbar/footer/search chrome — the brief's cart-drawer/`app.js` chrome-injection guards are vanilla-specific and N/A here. **Page** ([page.tsx](../src/app/learn/sizing/page.tsx) — server component, `export const metadata`, prerenders **static `○`**): hero (breadcrumb Home/Sizing Guide + eyebrow + display headline + intro + 6 jump-link chips) then six `scroll-mt` sections — **(1) Gauge:** "higher number = thinner" copy, a **visual bar scale** (20G→10G, pink gradient bars widthed by `thickness/2.4`), and a conversion table (gauge·mm·inches·uses) that is a real `<table>` on desktop and **stacks into labeled cards** (`max-[720px]`) on mobile, + safety note; **(2) Diameter & length:** two cards with **inline-SVG diagrams** (ring = inner Ø, barbell = wearable length) + common-size chips (6/8/10/12mm); **(3) By placement:** 12 cards (earlobe→industrial) each with gauge/size `Spec` chips, styles, and a fit note, grid reflow 4→3→2→1; **(4) Measure at home:** 4 numbered step cards; **(5) Safe by material:** 6 cards **color-coded by fresh-safety** (green fresh-safe / gold healed-only / red avoid-in-fresh, via `TIER_STYLE`); then the **FAQ** accordion + a pink-gradient "shop the collection" CTA → `/products`. **Content** is a single editable module ([data.ts](../src/lib/sizing/data.ts)) — `GAUGES/SIZE_CHIPS/PLACEMENTS/STEPS/MATERIALS/SIZING_FAQS` typed arrays (plain static, client-safe). **FAQ** ([sizing-faq.tsx](../src/components/sizing/sizing-faq.tsx)) is a `"use client"` accordion reusing the [home/faq.tsx](../src/components/home/faq.tsx) rotating-plus pattern (single-open, `aria-expanded`/`aria-controls`, `max-height` transition). **Decisions (locked at `start`):** (1) route **`/learn/sizing`** (matches the footer link); (2) **WhatsApp "size you for free" callout excluded** per user — Section 4 is just the 4 steps (the WhatsApp FAB stays deferred site-wide); (3) **page + footer link only** — product-page "Sizing guide" links **not** wired (deferred); (4) footer social edits **carried onto this branch** (see below). **Reveal-animation pivot (during `/verify`):** the first pass used IntersectionObserver + `opacity-0` scroll-gating, but a full-page screenshot proved below-the-fold content stayed **invisible** (observer never fires when the viewport sits at top) — a real no-JS/print/SEO hazard. Rewrote [reveal.tsx](../src/components/sizing/reveal.tsx) as a **pure-CSS mount entrance** (a plain server component applying `animate-fade-up`; content always rendered visible since the `fadeUp` keyframe's `both` fill ends at opacity 1) + a **`prefers-reduced-motion` guard** in [globals.css](../src/app/globals.css) (`--animate-fade-up` token + `fadeUp` keyframe + reduced-motion `animation:none`). **Runtime-verified** (`/verify` — Playwright vs. dev server, dark theme): all six sections + hero + CTA render; gauge bar scale + conversion table show and the **table stacks into labeled MM/INCHES/USES rows at 375px**; 12 placement cards + ring/barbell SVGs + 6 color-coded material cards present; **FAQ accordion toggles** (clicked item → `[expanded]`, prior closes, `aria-expanded` correct); grids reflow to 1 col on mobile; **0 console errors** (only the inherited root-layout `viewport`-in-metadata warning). **Lint + production build clean** (`/learn/sizing` static; the React-19 `set-state-in-effect` was avoided by dropping the observer entirely); `npm test` still 7/7. **Light theme** relies on `light:` classes (dark runtime-verified; light not toggle-tested this pass). **Bundled (per the `start` decision — unrelated footer edits from the prior task):** [footer.tsx](../src/components/layout/footer.tsx) — real Instagram (`instagram.com/sherryberries_/`) + TikTok (`tiktok.com/@sherrybvanessa`) URLs, external links now `target="_blank" rel="noopener noreferrer"` (icon row + external `FooterCol` links), and the "Follow" column trimmed to Instagram + TikTok (Pinterest/YouTube/Berry List removed). **Out of scope (v2, per PRD):** interactive size finder, printable mm ruler, per-product size availability, mm/inches units toggle, account-saved sizing profiles.
- 2026-07-19 — **R2 Product Image Upload (admin)** → [src/lib/storage/r2.ts](../src/lib/storage/r2.ts), [src/app/api/admin/upload/route.ts](../src/app/api/admin/upload/route.ts), [src/components/admin/product-form.tsx](../src/components/admin/product-form.tsx), [next.config.ts](../next.config.ts), [.env.example](../.env.example), plus first-time test harness ([vitest.config.ts](../vitest.config.ts), [test/empty-module.ts](../test/empty-module.ts), [src/lib/storage/r2.test.ts](../src/lib/storage/r2.test.ts)). Turned the admin product form's **URL-only image field into a real file upload** backed by Cloudflare R2 — the `R2_*` env vars existed in `.env.example` but **nothing in the codebase read them** (no S3 SDK, no upload route). **Storage lib** ([r2.ts](../src/lib/storage/r2.ts), `server-only`): a lazily-instantiated `@aws-sdk/client-s3` `S3Client` pointed at `https://<R2_ACCOUNT_ID>.r2.cloudflarestorage.com` (`region: "auto"`, R2 access-key creds); `uploadProductImage(bytes, contentType)` PUTs to `products/<randomUUID>.<ext>` with an `immutable` cache header and returns `${R2_PUBLIC_URL}/<key>`; pure `validateImageUpload(contentType, size)` (jpeg/png/webp/gif, ≤5 MB, non-empty — **type checked before size**) split out for testability; `isR2Configured()` gates on all five env vars. **Endpoint** ([upload/route.ts](../src/app/api/admin/upload/route.ts)): `POST /api/admin/upload`, `requireAdmin()` (403 otherwise), multipart `file` field → validate → upload → `{ url }`; returns **503 "paste a URL instead"** when R2 is unconfigured (graceful degradation), 400 on bad/missing file, 500 on R2 error. **Form** ([product-form.tsx](../src/components/admin/product-form.tsx)): "Upload image" button + hidden `<input type=file accept=…>` beside the preview thumb; on select, POSTs a `FormData` to the endpoint and drops the returned URL into the existing `imageUrl` field — **the URL then flows through the unchanged products save path** ([products/route.ts](../src/app/api/admin/products/route.ts)) into `ProductImage.imageUrl` (position 0). URL-paste box **kept as a fallback**; live preview, "Uploading…" busy state, inline upload errors, submit disabled mid-upload. **next.config** ([next.config.ts](../next.config.ts)): storefront `next/image` `remotePatterns` gains the R2 host — derived from `R2_PUBLIC_URL` at config load **plus** a `**.r2.dev` wildcard (default public-bucket host) — so uploaded photos render on the storefront, not just the admin `<img>` thumb. **Decisions (locked at `start`):** (1) **server-proxied** upload (file → our API → R2), R2 keys stay server-only; (2) **single image** (matches the current form + storefront usage); (3) **keep URL-paste fallback**; (4) accept jpeg/png/webp/gif ≤5 MB; new dep `@aws-sdk/client-s3`. **Test harness bootstrapped (repo's first):** the project had **no runner** despite CLAUDE.md/ai-interaction.md describing `npm test` (no `vitest` dep, no `test` script, zero `*.test.ts`) — added `vitest`, `test`/`test:watch` scripts, and [vitest.config.ts](../vitest.config.ts) (Node env, `include: src/**/*.test.ts`) that **stubs `server-only`/`client-only`** ([test/empty-module.ts](../test/empty-module.ts)) so any `server-only` lib/action can be unit-tested going forward. [r2.test.ts](../src/lib/storage/r2.test.ts): **7 passing** tests over `validateImageUpload` (every accepted type, exact-limit, unsupported type, svg, empty, one-over-limit, type-before-size ordering). **Lint + production build clean** (`/api/admin/upload` registered `ƒ` dynamic; only the pre-existing `ProductReviews` unused-import warning + inherited root-layout `viewport` warning remain); `npm test` → 7/7 green. **Not runtime-verified against a live R2 bucket** — needs real `R2_*` creds + a signed-in admin session; degrades gracefully (503 + URL fallback) without them. **Known follow-ups (out of scope):** deleting a product/image row leaves the R2 object **orphaned** (no cleanup on delete); content-type is browser-reported, not magic-byte verified (acceptable behind the admin trust boundary); single image only (schema supports a gallery).
- 2026-07-28 — **Contact Page (`/contact`) + owner-notification email (`POST /api/contact`)** → [src/app/contact/page.tsx](../src/app/contact/page.tsx), [src/components/contact/contact-form.tsx](../src/components/contact/contact-form.tsx), [src/lib/contact/validate.ts](../src/lib/contact/validate.ts), [src/lib/contact/validate.test.ts](../src/lib/contact/validate.test.ts), [src/app/api/contact/route.ts](../src/app/api/contact/route.ts), [src/lib/email/resend.ts](../src/lib/email/resend.ts), [src/lib/rate-limit.ts](../src/lib/rate-limit.ts), [.env.example](../.env.example). Fills the footer's previously-dead **"Contact" → `/contact`** link (flagged in the launch review) and adds the site's **first inbound email path** — every prior Resend sender flowed *outward* to customers (verification / password-reset / order-confirmation); nothing emailed the owner. **Page** ([page.tsx](../src/app/contact/page.tsx) — server component, `export const metadata`, prerenders **static `○`**): breadcrumb (Home / Contact), hero (`Get in touch` eyebrow + display **"Say *hello*."** + intro), then a `1.4fr / 0.6fr` grid (stacks at `900px`) of a rounded form card + an aside ("Response time" — 1–2 business days, include your order number; "Prefer socials?" — real Instagram + TikTok links, `target="_blank" rel="noopener noreferrer"`), reusing `globals.css` theme tokens (`bg-card`/`border-line`/`text-ink-dim`/`pink`/`blush`) + the global navbar/footer/search chrome. **Form** ([contact-form.tsx](../src/components/contact/contact-form.tsx) — `"use client"`): name, email, **optional** subject, message; a `Status = "idle" | "sending" | "sent" | "error"` machine drives a client-side `validateContact` pre-check → inline red error, a disabled **"Sending…"** button, a success state with a "send another" reset back to `idle`, and a graceful error surfacing the route's message. **Pure validator** ([validate.ts](../src/lib/contact/validate.ts)) — no I/O, no `server-only` imports, so the **same function runs on both sides**: `CONTACT_LIMITS` (name ≤100, subject ≤150, message 10–5000), a deliberately permissive `EMAIL_RE` (real proof of a valid inbox is delivery), returning `{ ok: true, data }` | `{ ok: false, error }` with friendly copy. Unit-tested in [validate.test.ts](../src/lib/contact/validate.test.ts) per the project's `src/lib/**`-only convention (**8 tests**; suite now **15/15** with the R2 7). **Endpoint** ([api/contact/route.ts](../src/app/api/contact/route.ts)): `contactLimiter` first (**5/hour per IP**, added to [rate-limit.ts](../src/lib/rate-limit.ts) via the same `Ratelimit.slidingWindow` helper + `checkRateLimit`/`getClientIp`/`tooManyRequests`, **failing open** when `UPSTASH_*` is unset, exactly like the auth endpoints) → JSON-parse guard (**400**) → `validateContact` (**400**) → `resolveDestination()` → `sendContactEmail` (**502** on provider failure, and the payload is `console.error`-logged in dev so a message isn't silently lost) → `{ ok: true }`. **Destination is env-driven, never hardcoded:** `CONTACT_EMAIL`, else the address parsed out of `EMAIL_FROM` (handles both `Name <a@b>` and a bare address), else **503** + a server-side error log. **Email** (`sendContactEmail` in [resend.ts](../src/lib/email/resend.ts)): mirrors the existing senders (same `FROM` const, `getClient()` null-guard, returns `{ ok, error }` and never throws); `from` stays the verified-domain sender and the visitor's address goes in **`replyTo`** so the owner can reply directly (you can't send *as* an unverified arbitrary address); subject `[Contact] <subject | "New contact form message">`; html + text alternatives, and the HTML template **escapes every user-supplied field** (`escapeHtml`, `\n` → `<br>`) so a hostile message can't inject markup into the owner's inbox. **Decisions (locked at `start`):** (1) destination = **env `CONTACT_EMAIL`** with an `EMAIL_FROM` fallback; (2) **email-only, no persistence** — no `ContactMessage` model, so **no schema change or migration**; (3) name/email/message **required**, subject **optional**; (4) **public/guest** submission, no auth; (5) **owner-only** delivery — no auto-acknowledgement email to the visitor (v1). **Verified:** `npm run lint` clean (only the pre-existing unrelated `ProductReviews` unused-import warning), `npm test` **15/15**, production build clean (`/contact` **static ○**, `/api/contact` registered **ƒ**), and live against the dev server — `/contact` → **200**, `POST {}` → **400** `"Please enter your name."`, invalid email → **400** `"Please enter a valid email address."`. **Not verified: an actual successful send** — deliberately not triggered, since a valid POST emails a real inbox. **Blocking caveat before this is useful in production:** `CONTACT_EMAIL` is **unset in `.env`**, so contact mail currently falls back to `onboarding@resend.dev`; Resend is still on that **test sender**, which only delivers to the Resend account owner until a real domain is verified. Set both before launch (`CONTACT_EMAIL` is documented in [.env.example](../.env.example) with this caveat). **Light theme** relies on `light:` classes (dark verified this pass; light not toggle-tested). **Bundled as separate commits (unrelated changes present in the working tree at `complete`, kept out of the feature commit):** (a) `f013afd` — [footer.tsx](../src/components/layout/footer.tsx) CARE-column dead links **"Healing Guide"** (`/aftercare-guide`) + **"Materials Glossary"** (`/learn/materials`) commented out (carried over from the prior task; both routes still don't exist), plus `/.playwright-mcp/` added to [.gitignore](../.gitignore) (30 untracked MCP browser console/page dumps — debug output, not source); (b) `ace51a4` — [wipay/return/route.ts](../src/app/api/checkout/wipay/return/route.ts) now dev-logs WiPay's `message` param + the full return querystring on a failed callback, added while debugging **sandbox card transactions that have failed on every attempt since 2026-07-18** returning `status="error"` with no recorded reason (see follow-up). **Out of scope (v2):** auto-acknowledgement email to the visitor, persisting submissions to a DB model + an admin inbox view, spam captcha/honeypot beyond the rate limit, file attachments, per-field inline validation messages.
- 2026-09-07 — **Jewelry lengths with per-length stock** → [src/lib/admin/sizes.ts](../src/lib/admin/sizes.ts) + [sizes.test.ts](../src/lib/admin/sizes.test.ts), and edits to [api/admin/products/route.ts](../src/app/api/admin/products/route.ts), [api/admin/inventory/route.ts](../src/app/api/admin/inventory/route.ts), [product-form.tsx](../src/components/admin/product-form.tsx), [inventory-view.tsx](../src/components/admin/inventory-view.tsx), [queries/admin.ts](../src/lib/queries/admin.ts), [admin/options.ts](../src/lib/admin/options.ts), [admin/audit-changes.ts](../src/lib/admin/audit-changes.ts). The product modal can now hold a repeatable list of sizes — an axis label on the product plus a value, quantity and optional price delta per row. **No migration was needed:** `ProductVariant` already modelled this exactly (`name` = the axis, `value` = the size, `inventory` = the quantity at it), and `prisma migrate diff` confirmed no schema change. Variants existed only because the seed created them — grepping for `variant` returned **zero** matches across all four admin files, so an admin could create a product but never give it sizes. **Decisions (owner, at `start`):** (1) the **axis label is per-product** rather than hard-coded, so the 40 seeded products labelled "Gauge" keep working while new ones can say "Length"; (2) **`Product.inventory` is derived** for a sized product — the sum of its sizes, recomputed on every save. Both numbers are live in checkout (an order line with a `variantId` decrements the variant, one without decrements the product), so drift is how a shop oversells; enforced in **three** places because one would leave the others silently disagreeing — the route recomputes it, the modal shows it read-only with a live sum, and the Inventory tab swaps its stepper for static text (its API returns 409 if asked anyway), with price still editable inline either way; (3) **removing an ordered size is refused** — neither `CartItem.variantId` nor `OrderItem.variantId` declares `onDelete` and Prisma defaults an optional relation to `SetNull`, so the delete would silently erase which size a customer bought; the error says to set the quantity to 0 instead, and a size only sitting in a cart may still go. Variant SKUs are **derived** from the product's own (`SB-ROSEGOLD14` + `8mm` → `SB-ROSEGOLD14-8MM`) so they follow a rename; surviving rows are parked on a temporary SKU first, or swapping two sizes' values collides with the unique index mid-save, and a cross-product collision (`SB-1`+`2` and `SB`+`1-2` both derive `SB-1-2`) returns a 409 naming the fix rather than a 500. Size writes audit as `product.sizes_update`, separate from `product.update`, inside the product's own transaction. **Left open:** 40 of 62 seeded products have a `Product.inventory` disagreeing with their sizes (the seed sets both independently), so the first modal save of one silently corrects it — a one-time reconciliation was offered and not yet run. The modal has not been exercised in a browser; verification was types, lint, 22 new unit tests (106 total), `next build`, and a 403 on an unauthenticated POST.
- 2026-09-08 — **Bank transfer payment with receipt verification** → [src/lib/checkout/bank-transfer.ts](../src/lib/checkout/bank-transfer.ts) + [.test.ts](../src/lib/checkout/bank-transfer.test.ts), [expire-orders.ts](../src/lib/checkout/expire-orders.ts), [release-stock.ts](../src/lib/checkout/release-stock.ts), [api/admin/payments](../src/app/api/admin/payments/route.ts), [api/orders/[orderNumber]/receipt](../src/app/api/orders/%5BorderNumber%5D/receipt/route.ts) + [/[receiptId]](../src/app/api/orders/%5BorderNumber%5D/receipt/%5BreceiptId%5D/route.ts), [order/[orderNumber]/payment](../src/app/order/%5BorderNumber%5D/payment/page.tsx), [bank-transfer-panel.tsx](../src/components/checkout/bank-transfer-panel.tsx), [payments-view.tsx](../src/components/admin/payments-view.tsx), migration `20260909004024_bank_transfer_payment`, and edits to [checkout/route.ts](../src/app/api/checkout/route.ts), [wipay/return/route.ts](../src/app/api/checkout/wipay/return/route.ts), [checkout-form.tsx](../src/components/checkout/checkout-form.tsx), [shipping.ts](../src/lib/checkout/shipping.ts), [email/resend.ts](../src/lib/email/resend.ts), [storage/r2.ts](../src/lib/storage/r2.ts), [rate-limit.ts](../src/lib/rate-limit.ts), [queries/admin.ts](../src/lib/queries/admin.ts), [admin-client.tsx](../src/components/admin/admin-client.tsx). Spec: [bank-transfer-spec.md](features/bank-transfer-spec.md) (35 sections). **The rule the feature protects:** a receipt is evidence the customer *claims* to have transferred money, not confirmation it arrived — uploading reaches `PAYMENT_SUBMITTED` and nothing more, and `/api/admin/payments` is the only path that writes `PAID`. **Decisions (owner, at `start`):** (1) **scope** was the core loop plus expiry, with emails added afterwards on request and admin notification (§30/§31) still outstanding; (2) **enum values additive, not renamed** — `PENDING` is load-bearing because the WiPay callback gates idempotency on it, so the spec's wording would have silently broken the guard against a replayed callback re-emailing or double-restocking; (3) **inventory stays decrement-on-create** rather than a reservation table — the existing `inventory: { gte: qty }` filter already gives §29's atomicity, and a reservation table would change every inventory read on the storefront, the admin Inventory view and the per-size stock feature; (4) **expiry is read-driven** (`expireOrderIfDue` per order, an awaited `sweepExpiredOrders` when the admin payload is built) since there is no cron and no `vercel.json` — nothing runs as an un-awaited promise, which a response can kill. **Found at `review` and fixed:** the rejection reason was unvalidated free text shown back to the customer, with the allowed list duplicated in the view and dead-exported from a route file; a **rejected order held its stock forever** (nothing expired it) — rejection now sets a fresh window and `REJECTED` joined `EXPIRABLE_STATUSES`; and the restock routine was duplicated from the WiPay failure path, now extracted to `releaseOrderStock()` and used by both, with WiPay keeping its cart rebuild at the call site. A unit test also caught `Number("")` being `0`, which made a blank `BANK_TRANSFER_PAYMENT_WINDOW_HOURS=` a **1-hour** window instead of 6. `PAYMENT_SUBMITTED` never expires on the clock (§18). Receipts are validated from the multipart type and byte length rather than the filename, stored under a random key with **no public-URL helper anywhere**, served only via an owner-or-admin route that 404s rather than 403s so it cannot confirm a receipt exists to someone who may not see it; the order is always resolved from the URL and checked against the session. **Left open:** `BANK_TRANSFER_BANK_NAME`/`_ACCOUNT_NAME`/`_ACCOUNT_NUMBER` are unset, so checkout still refuses with a 503 by design and **the flow has never been exercised end to end**; no order-created email, so a customer who closes the browser must find the order via /account; no admin notification. Verification was 139 unit tests, `tsc`, lint, `next build`, and every route's guard checked by hand.
