# Current Feature

## Status
Not Started

## Goals
<!-- Bullet points of what success looks like -->

## Notes
<!-- Additional context, constraints, or details from spec -->

---

## History
- 2026-06-04 — **Single-page Checkout at `/checkout` + in-page Thank-You (real orders + Resend email)** → [src/app/checkout/page.tsx](../src/app/checkout/page.tsx), [src/app/api/checkout/route.ts](../src/app/api/checkout/route.ts), [src/lib/checkout/shipping.ts](../src/lib/checkout/shipping.ts), [src/components/checkout/](../src/components/checkout/) (checkout-client, checkout-form, checkout-summary, thank-you, shared), [src/lib/email/resend.ts](../src/lib/email/resend.ts), [src/components/providers/cart-provider.tsx](../src/components/providers/cart-provider.tsx), [src/components/cart/cart-client.tsx](../src/components/cart/cart-client.tsx), [src/proxy.ts](../src/proxy.ts). Server shell `/checkout` (auth-gated like `/wishlist`/`/account`: `auth()` → `redirect("/login?callbackUrl=/checkout")`, prefills contact + default address) → client `<CheckoutClient>`: back-to-bag link + **step indicator** (Bag done → Checkout active → Confirmation upcoming) + Italiana **"Secure checkout."**; `1fr/400px` layout (stacks <940px) with a **4-section form** ([checkout-form.tsx](../src/components/checkout/checkout-form.tsx)) — Contact (first/last + email/phone), Shipping address (address/city/optional landmark), **Shipping method** (3 custom radio cards: Pickup Curepe **Free**/TTPost **$25**/Courier **$40**, icon+title+sub+price, selected = pink ring + filled dot), **Payment** (COD + Credit Card w/ WiPay tag → `grid-rows` card-fields reveal + green-lock note) — and a **sticky pink Order summary** ([checkout-summary.tsx](../src/components/checkout/checkout-summary.tsx)): 56px thumb + pink qty badge, promo row → "✦ CODE applied" chip, totals (subtotal, blush discount, shipping label + green "Free", Playfair grand total), gold→pink Place-order button + secure note. Place-order validation (first/last/email-regex/phone≥7/address/city; red field + per-field msg + focus-first + toast) → POST → swaps to the **Thank-You** state ([thank-you.tsx](../src/components/checkout/thank-you.tsx)): pop-in checkmark + radial glow, "Thank you, *{first}*!", confirmation-to-email line, pink order card (number + Processing badge; Date/Items/Ship to/Method/Payment/optional Discount/Total), pink next-steps strip ("{eta}. We'll text {phone}…" + COD-only "have payment ready on arrival"), **Track my order →** `/account?view=orders` + **Continue shopping →** `/products`. **Real, auth-gated order creation:** `POST /api/checkout` recomputes totals from the **DB cart** (never trusts client prices), re-validates the promo against `DiscountCode`, then in **one `$transaction`** creates `Order`+`OrderItems`, **atomically compare-and-decrements** the gating stock counter (`updateMany … where inventory ≥ qty`; `count===0` → `OversoldError` → roll back → **409**, so concurrent orders can't oversell — decrements variant stock if a variant was chosen, else product, mirroring the availability check), empties the cart, and bumps `DiscountCode.timesUsed`; then sends a **best-effort Resend confirmation email** ([sendOrderConfirmationEmail](../src/lib/email/resend.ts) — dark/pink template w/ line-item table + totals; order never fails on email trouble; dev-logs the send). **Decisions (locked at `start`):** (1) **real + auth-gated** (`Order.userId` is required) **+ confirmation email with items**; (2) **checkout per brief: flat T&T shipping, NO tax** — which fits the `Order` schema (cols `subtotal`/`shippingCost`/`total` only; **no tax/discount/address columns**), so discount stays *derived* and the typed shipping details are **snapshotted into `Order.notes`** (no migration); the existing `/cart` summary keeps its 8.75%-tax + free-ship-≥$80 display, so **the grand total can differ between `/cart` and `/checkout`** (accepted trade-off); (3) **inventory decremented** in the transaction. Other defaults: COD → `paymentStatus PENDING`, mock card → `PAID` (UI-only, no real WiPay); new orders `fulfillmentStatus UNFULFILLED` → render as "Processing" in `/account`; `orderNumber` = `SB-`+8 digits (matches seed); **shared [shipping.ts](../src/lib/checkout/shipping.ts)** single-sources fees/labels/ETAs across API + client. **Cart hand-off:** `/cart` "Secure checkout" now persists any applied promo to `sessionStorage["sb-promo"]` and `router.push("/checkout")` (was a mock toast); checkout re-hydrates + **re-validates** that promo via `/api/promo`; `CartProvider` gained **`clearCart`** so the navbar bag badge resets post-order. `/checkout` added to the proxy matcher (defense-in-depth alongside the page guard). **Deviations:** variant line shows `variantValue`/category, not literal "material · size · stone" (single-value variant schema; server-side order + email *do* include `material · value`); card fields not validated (mock); "Continue shopping" → `/products` (not home `#shop`); literal `checkout.html`/`.js`/`styles.css` not created (stack-translated); WhatsApp FAB still deferred. **Lint + prod build clean**; `/checkout` is `ƒ` dynamic, `/api/checkout` registered; the `/checkout` `viewport`-in-metadata warning is the inherited root-layout one (same as `/cart`,`/wishlist`). **Verification note:** **not runtime-verified in a browser** — needs a signed-in session with cart items; during `complete` the environment had **Neon Postgres + GitHub both unreachable** (network/DB outage — the homepage threw `Can't reach database server` on `getHomeCategories`, unrelated to this code). Worth a manual pass once connectivity returns: sign in as `admin@sherryberries.test`, add a product, place an order (COD + a promo like `BERRY10`), confirm Thank-You + the order appearing in `/account` → Orders + stock decrement. **PENDING PUSH:** merged to local `master` (`164a130`) but **not pushed to origin** (GitHub was unreachable at completion) — run `git push origin master` once the network is back.
- 2026-06-04 — **My Account area at `/account` (logged-in only)** → [src/app/account/page.tsx](../src/app/account/page.tsx), [src/lib/queries/account.ts](../src/lib/queries/account.ts), [src/app/api/account/profile/route.ts](../src/app/api/account/profile/route.ts), [src/app/api/account/addresses/route.ts](../src/app/api/account/addresses/route.ts), [src/app/api/account/password/route.ts](../src/app/api/account/password/route.ts), [src/components/account/](../src/components/account/) (account-client, shared, order-views, returns-view, addresses-view, profile-view, security-view). Server shell `/account` (mirrors `/wishlist`: `auth()` → `redirect("/login?callbackUrl=/account")`, then fetch) → client `<AccountClient>` orchestrating **6 sidebar views + 1 hidden order-detail view**: sticky sidebar (gradient-initials avatar, name, "★ Sweet Berry" tier, icon nav w/ live **Orders/Returns count badges**, active = pink-gradient+blush+border, red-hover Sign out → `signOut({callbackUrl:"/login"})`, horizontal-scroll on mobile); header "Hello, *first*." (Italiana + blush italic, driven by profile state); **Dashboard** (3 stat cards + recent-4 orders); **Orders** (rows w/ status badge, date+payment, ≤4 thumbnails + "+N", Playfair total, Track hidden when Cancelled); **Order detail** (back link, Order-info + Shipping cards w/ dashed rows, Items + totals incl. optional blush discount + Playfair grand total, **5-stage tracking timeline** Pending→Delivered w/ done/current[glow]/pending dots + filled bars — skipped when Cancelled, "Request a return" → Returns prefilled); **Returns** (eligible-order → product → reason → notes form w/ validation → `RT-####` prepend + toast; list w/ status badges + empty state); **Addresses** (2-col grid, default pink-tinted + "✦ Default", Set-default/Edit/Delete + dashed add tile, add/edit modal validating name+line1+city, single-default flip, delete-confirm + promote-first); **Profile** (form validating email+phone → persist + live header/sidebar update + "Member since {date}"); **Security** (change-password validation + red danger-zone delete-account confirm modal → toast → redirect home ~1.8s). Centralized **modal** (scrim+box, scale/translate, scrim-click + Escape close) + **toast**; **`?view=` deep-linking** (default Dashboard). **Data — hybrid real Prisma:** [account.ts](../src/lib/queries/account.ts) `getAccountData(userId)` serializes the signed-in user's real `User`/`Address`/`Order`+`OrderItem` and **derives the presentational gaps** — display status from `FulfillmentStatus`, 5-stage tracking index, `discount = max(0, subtotal+shipping−total)`, `returnEligible = DELIVERED`, `memberSince` from `User.createdAt` (Decimals → `Number()`; "Packed" stage has no enum member so it only reads done once shipped). **Mutations** via new auth-gated routes: `PATCH /api/account/profile` (unique-email guard), `POST/PATCH/DELETE /api/account/addresses` (single-default `$transaction`, `makeDefault` flip, promote-on-delete; maps spec fields → schema `fullName`/`region`/`postalCode`/`addressLine*`), `POST /api/account/password` (bcrypt-compare current → hash new). **Decisions / deviations:** (1) **Returns is client-state only** (sessionStorage `sb-acct-returns`, seeded with 2 demo requests from the user's real orders on first visit) — no schema model, the one mock piece, per the `start` decision; (2) cross-view actions are **React callbacks**, not the spec's vanilla `data-attribute` delegated click listener; (3) **ship-to = the account's default address for every order** — `Order` carries no address snapshot (schema gap, flagged at `start`); (4) **tier "Sweet Berry"** is presentational (no backing field); (5) **delete-account is a mock** (toast → redirect, no deletion endpoint) — matches the spec's "scheduled for deletion" wording; (6) literal `account.html`/`account.js`/`data.js`/`styles.css` **not** created (stack-translated to Next/React/TS/Tailwind, per Login/Cart/Wishlist convention); (7) **WhatsApp FAB** not added (deferred site-wide, consistent with cart/wishlist). **Lint + prod build clean**; `/account` is `ƒ` dynamic (calls `auth()` + DB), all three `/api/account/*` routes registered. The three `setState`-in-effect calls (returns hydration, `?view=` read, returns prefill) are wrapped in `queueMicrotask` per the project's React 19 no-sync-setState-in-effect convention. **Known limitation:** changing email via Profile updates the DB but the session JWT keeps the old email/name until next sign-in (no user-visible breakage — navbar shows neither). **Verification note:** interactive browser `/verify` was **not completed** — the run was interrupted and a live smoke-test against the dev server was inconclusive in this shell (port-3000 conflict with an existing dev instance + localhost/IPv4 connectivity); completion rests on clean lint + production build + code review, not runtime observation. Worth a manual pass at `/account` (sign in as `admin@sherryberries.test` — if that user has no seeded orders/addresses, the Dashboard/Orders/Addresses empty states render instead of populated data).
- 2026-06-01 — **Forgot Password (Reset via Resend email)** → [src/lib/auth/password-reset.ts](../src/lib/auth/password-reset.ts), [src/lib/email/resend.ts](../src/lib/email/resend.ts), [src/app/api/auth/forgot-password/route.ts](../src/app/api/auth/forgot-password/route.ts), [src/app/api/auth/reset-password/route.ts](../src/app/api/auth/reset-password/route.ts), [src/app/forgot-password/page.tsx](../src/app/forgot-password/page.tsx), [src/app/reset-password/page.tsx](../src/app/reset-password/page.tsx), [src/components/auth/forgot-password-form.tsx](../src/components/auth/forgot-password-form.tsx), [src/components/auth/reset-password-form.tsx](../src/components/auth/reset-password-form.tsx). The login page's pre-existing "Forgot password?" link (→ `/forgot-password`) now resolves to a real flow. **Token helper** ([password-reset.ts](../src/lib/auth/password-reset.ts)) mirrors the email-verification pattern but **reuses the `VerificationToken` table under a `reset:<email>` identifier namespace** — so **no migration** and the two flows' `deleteMany`/consume never clobber each other (32-byte hex, **1h TTL**, single-use): `createPasswordResetToken` (clears prior reset tokens, mints fresh), `findValidResetToken` (non-consuming check for page render), `consumePasswordResetToken` (re-validates → bcrypt-hashes → updates `User.password` → deletes token; clears expired tokens on the way out). **Email:** `sendPasswordResetEmail` added to [resend.ts](../src/lib/email/resend.ts) (matching dark/pink HTML template, "expires in 1 hour" copy). **`POST /api/auth/forgot-password`** is **enumeration-safe** — one generic reply (`"…check your inbox for the next step."`) for every account state — and branches on account state: **verified** account → sends the reset link; **unverified** account → sends a *verification* link instead (verify-first, reusing `createVerificationToken` + `sendVerificationEmail`), so resetting is gated on the same email-ownership proof as sign-in; **no account** → nothing. Dev console-logs the URL (`[reset-password]` / `[reset-password → verify-first]`). **`POST /api/auth/reset-password`** validates token + password (≥8, match), consumes the token, sets the new password (`400` + `reason: invalid|expired` on failure). **Pages:** `/forgot-password` (server shell + `ForgotPasswordForm` client — email input → generic confirmation explaining the verify-first path); `/reset-password` (server-validates token **non-consuming** on render → renders `ResetPasswordForm` with strength meter + confirm field + success→sign-in CTA when valid, else an invalid/expired card with a "request a new link" CTA). **Token consumed on submit, not link-click** (opening the link only validates/renders the form). Lint + prod build clean; `/reset-password` is `ƒ` dynamic (awaits `searchParams` + DB lookup), `/forgot-password` is static. **Decisions (per follow-up requests):** (1) reset **does not** touch `emailVerified` — kept verification as its own chokepoint so reset can't be a side-door past the login gate ("leave as-is" chosen); (2) reset is **gated on `emailVerified`** + unverified requesters are routed to verify-first; (3) skipped the optional polish (distinct expired-vs-invalid page state; text "Show/Hide" instead of the login page's eye-icon SVGs). **Notes:** Resend test sender only delivers to the account owner until a domain is verified — console URL is the dev path; the `/reset-password` `viewport`-in-metadata build warning is the pre-existing root-layout warning (also fires on `/verify-email`, `/wishlist`), inherited not introduced. **Low-severity edge:** a reset token manually pasted into `/verify-email` would 500 (no user matches the `reset:` identifier) — only reachable by hand-editing a URL across flows; normal emails never cross-link.
- 2026-06-01 — **Full-page Wishlist at `/wishlist` (logged-in only)** → [src/app/wishlist/page.tsx](../src/app/wishlist/page.tsx), [src/components/wishlist/](../src/components/wishlist/) (wishlist-client, wish-card, wish-recs, wish-empty), [src/app/api/wishlist/snapshot/route.ts](../src/app/api/wishlist/snapshot/route.ts), [src/proxy.ts](../src/proxy.ts), [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx), [src/components/home/wishlist-button.tsx](../src/components/home/wishlist-button.tsx), [src/components/home/bestsellers.tsx](../src/components/home/bestsellers.tsx), [src/lib/queries/home.ts](../src/lib/queries/home.ts), [src/components/product/product-wishlist-button.tsx](../src/components/product/product-wishlist-button.tsx). Server shell `/wishlist` → client `<WishlistClient>` (mirrors `/cart`): **bulk bar** ("N pieces saved" + running total of all saved prices), **toolbar** (auto-generated category filter chips, active inverts to solid `--ink`, + "Showing X of Y"), responsive 4→3→2→1 **grid** of `<WishCard>` (1.06× image zoom, full-cover PDP link, Bestseller/New tag pill, glass remove-heart, "Added X ago" badge, ratings, material/tag chips, struck-through price, pink **Add** → green "✓ In bag"/navigate-to-cart), 300ms `.removing` scale-0.92/fade with optimistic snapshot filter, **recs** rail (featured products not already wishlisted, each with a save heart), **empty state**, and header **Share** (clipboard) + **Add all to bag**. **APIs:** new `POST /api/wishlist/snapshot` enriches the user's DB wishlist (image, price, compareAtPrice, category, rating, material+tag chips, Bestseller/New pin, `addedAt` from `Wishlist.createdAt`); recs pool from `listProducts()` (featured, top 12) filtered client-side. Reuses the real `WishlistProvider`/`CartProvider` and the global navbar/footer chrome; toast + light-theme parity copied from `/cart`. **Decisions:** no `sessionStorage["sb-wish"]` / no demo-seed (spec's vanilla constructs dropped — real Prisma + providers); "Added X ago" derived from DB `createdAt` (signed-in only). **Auth-gated (per follow-up request "wishlist only for logged-in users" + "hide wishlist for non-logged-in users"):** `/wishlist` protected by the proxy matcher *and* a page-level `auth()` → `redirect("/login?callbackUrl=/wishlist")`; snapshot endpoint 401s guests; every wishlist heart (navbar desktop+mobile, homepage bestsellers cards, PDP "Save") renders only when `useSession().status === "authenticated"` (hidden during `"loading"` → server/first-client render agree, no hydration mismatch). **Bug fixed mid-feature:** the homepage bestsellers heart ([home/wishlist-button.tsx](../src/components/home/wishlist-button.tsx)) was **cosmetic-only** (local `useState`, no persistence) — the user reported toggling it didn't populate the wishlist; rewired it to `useWishlist().toggle(productId)` with `isWishlisted` state + a `busy` guard, so every heart now writes to the `Wishlist` table. `getBestsellers` now returns product `id`. Lint + prod build clean. **Notes:** `/wishlist` is `ƒ` dynamic (calls `auth()`); the pre-existing root-layout `viewport`-in-metadata warning ([layout.tsx:39](../src/app/layout.tsx#L39)) is unrelated and left as-is. Navbar "Learn"→"FAQ" relabel (href `/faq`) was a concurrent user edit included in the same commit.
- 2026-05-31 — **Products Navbar Dropdown** → [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx). The navbar "Products" link became a dropdown with three children: **Jewelry** → `/products`, **Aftercare** → `/products?category=aftercare` (same href the footer's "Aftercare Shop" uses), **Merchandise** → `/products?category=merch`. `NAV_LINKS` items gained an optional `children: NavChild[]`; the desktop link/mobile-link classNames were extracted to module consts (`desktopLinkClass`/`mobileLinkClass`) so the dropdown trigger matches the other nav items. **Desktop:** new `NavDropdown` client component — opens on hover (120ms close delay via `closeTimer` ref to avoid flicker) and on click; chevron (`ChevronDownIcon`) rotates 180° when open; closes on `Escape`, outside `mousedown`, or item select; accessible via `aria-haspopup="menu"`/`aria-expanded`/`aria-controls` + `role="menu"`/`menuitem`. Panel conditionally rendered (not just hidden) so closed items aren't tab-focusable; styled to match the glass pill (`backdrop-blur`, pink-accent shadow, `bg-[rgba(15,12,13,0.97)]` + light-theme variant). **Mobile:** the three children render inline as indented sub-links (left border, `text-ink-faint`, smaller tracking) under a "Products" header link inside the existing menu panel — no hover dependency. Lint + prod build clean. **Notes:** "Jewelry" has no dedicated category slug, so it maps to the all-products view; kept the `border-white/[0.06]` bracket-opacity form to match the file's convention despite the canonical-class lint suggestion.
- 2026-05-27 — **Email verification on registration (Resend)** — [src/lib/email/resend.ts](../src/lib/email/resend.ts), [src/lib/auth/verification.ts](../src/lib/auth/verification.ts), [src/app/api/auth/register/route.ts](../src/app/api/auth/register/route.ts), [src/app/verify-email/page.tsx](../src/app/verify-email/page.tsx), [src/app/api/auth/resend-verification/route.ts](../src/app/api/auth/resend-verification/route.ts), [src/auth.ts](../src/auth.ts), [src/app/login/page.tsx](../src/app/login/page.tsx). New customers are created **unverified** (`User.emailVerified = null`); register mints a one-time token (`VerificationToken`, identifier = email, 32-byte hex, 24h TTL, old tokens cleared) and emails a verify link via Resend (`sendVerificationEmail`, from `EMAIL_FROM` default `SherryBerries <onboarding@resend.dev>`); dev also `console.log`s the URL. `/verify-email?token=…` consumes the token (sets `emailVerified = now()`, deletes token) and renders success/expired/invalid with a resend affordance. `authorize` blocks sign-in for unverified users ([src/auth.ts:36](../src/auth.ts#L36) `if (!user.emailVerified) return null;`); register returns `{ requiresVerification: true }` and the login page shows "check your email" instead of auto-signing-in. `resend-verification` re-mints for an unverified email (enumeration-safe no-op otherwise). No migration (`VerificationToken` + `User.emailVerified` already in schema). **Notes:** Resend test sender only delivers to the account owner — console URL is the dev path until a domain is verified; seeded customers with `emailVerified = null` are now blocked from login (expected). Shipped in commit `f1e7b4b` (was left marked *In Progress* in this file; recorded here retroactively).
- 2026-05-26 — **Full-page Shopping Cart at `/cart`** — [features/cart-spec.md](features/cart-spec.md) → [src/app/cart/page.tsx](../src/app/cart/page.tsx), [src/components/cart/](../src/components/cart/) (cart-client, cart-row, order-summary, saved-for-later, cart-empty, trust-strip), [src/app/api/cart/route.ts](../src/app/api/cart/route.ts), [src/app/api/cart/snapshot/route.ts](../src/app/api/cart/snapshot/route.ts), [src/app/api/promo/route.ts](../src/app/api/promo/route.ts), [src/lib/cart/local-extras.ts](../src/lib/cart/local-extras.ts), [src/components/providers/cart-provider.tsx](../src/components/providers/cart-provider.tsx), [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx). Server shell `/cart` → client `<CartClient>` (computes totals: subtotal, promo discount honoring `percentageOff`|`amountOff`, $6/line gift wrap, free shipping at `taxable ≥ $80` else $6, 8.75% tax; owns toast + the header free-shipping progress line). Cart row: image, variant chip, stock badge (green / amber "Only N left" / red), qty stepper 1–10, price with `compareAtPrice` strikethrough + "$X each" caption when qty>1. **Remove** is a prominent 36×36 circular X button at the row's top-right (pink-gradient on hover) — initial build buried it as a tiny ghost link which the user couldn't find; moved it out + dropped the duplicate. Removal does a 280ms fade then an **optimistic `setSnapshot` filter** in the same render as clearing `removingKeys`, so the row doesn't flash back before the snapshot refetch (the bug that made remove look broken). Sticky order summary with promo input (chip when applied, Enter-to-apply), totals breakdown, Afterpay caption, mock checkout (`Redirecting…` → toast), express stubs (Shop Pay / PayPal), perks. Saved-for-later strip (localStorage `sb-saved`, "+" moves back to bag). Gift wrap toggle (localStorage `sb-cart-gift`, keyed `productId::variantId`). Empty state + T&T-localized trust strip. **APIs:** `PATCH /api/cart` (set qty 1–10), `DELETE /api/cart?productId&variantId` (remove line), `POST /api/cart/snapshot` (unified enrichment — auth reads DB cart, guest enriches body items), `POST /api/promo` (validate against real `DiscountCode`, case-insensitive, checks active/expiry/usage limit). `CartProvider` gained `updateQuantity` + `removeLine` (auth → API, guest → sessionStorage). Navbar Bag CTA is now a `<Link href="/cart">` (was a dead button). Live curl-verified: `/cart` 200, promo `BERRY10` → `{percentageOff:10}`, bad code → 404, snapshot enriches a guest line, PATCH/DELETE → 401 for guests. **Deviations from spec:** vanilla-JS/HTML translated to React/Next; hardcoded promo codes (`SWEET10`/`BERRY20`/`FRESH15`) replaced with the seeded DB codes (`BERRY10`/`AFTERCARE15`/`WELCOME20`/`GLOWUP25`/`STUDIO50`); "ships from Brooklyn" → Trinidad. **Deferred:** real checkout + Shop Pay/PayPal SDKs (mocks), saved-for-later & gift-wrap DB persistence (localStorage only — no `CartItem` schema change), WhatsApp FAB ([whatsapp-toast.md](features/whatsapp-toast.md)). **Lint note:** guest-hydration + empty-cart setStates wrapped in `queueMicrotask` to satisfy React 19's no-sync-setState-in-effect rule (recurring pattern across providers/cart components).
- 2026-05-25 — **Product Detail Page (PDP) v1 + Products listing + Sign in/out + Auth-driven providers** — [features/product-page-spec.md](features/product-page-spec.md) → [src/app/products/[slug]/{page,loading,not-found}.tsx](../src/app/products/%5Bslug%5D/page.tsx), [src/app/products/page.tsx](../src/app/products/page.tsx), [src/app/api/cart/route.ts](../src/app/api/cart/route.ts), [src/app/api/wishlist/route.ts](../src/app/api/wishlist/route.ts), [src/lib/queries/product.ts](../src/lib/queries/product.ts), [src/components/product/](../src/components/product/), [src/components/providers/cart-provider.tsx](../src/components/providers/cart-provider.tsx), [src/components/providers/wishlist-provider.tsx](../src/components/providers/wishlist-provider.tsx), [src/components/auth/auth-button.tsx](../src/components/auth/auth-button.tsx), [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx). **PDP** at `/products/[slug]`: server-component page with `generateMetadata` (seoTitle/Description with name/shortDescription fallbacks + first image as og:image), breadcrumb (Home / Category / JewelryType / Name), `1.15fr / 1fr` hero grid (stacks <900px). Gallery: thumbnail strip (vertical desktop / horizontal scroll mobile), active glow, mouse-tracked hover zoom (scale 1.6×, `transform-origin` from cursor), Bestseller pill when tagged. Buy-box: variant chips with disabled+line-through when `inventory <= 0` and inverted-active state; qty stepper 1–10; live `final = base + variant.additionalPrice` price; in-stock / low-stock / out-of-stock pill; toast on add; mobile sticky bar via `IntersectionObserver` on the main CTA, only renders <880px. Accordion (5 sections, first open by default, rotating + icon, sliding `grid-template-rows` animation). Reviews: average + 5-row breakdown bars + up to 6 verified cards. Related products: 4-up grid keyed off same category OR jewelryType, featured-first. **APIs:** `POST/GET /api/cart` (auth-gated; merges duplicate productId+variantId, caps qty at 10) and `POST/GET /api/wishlist` (toggle via `userId_productId` compound key). **Providers** (`CartProvider`, `WishlistProvider`) mounted in [layout.tsx](../src/app/layout.tsx) inside `AuthSessionProvider`: auth path fetches API, guest path persists to sessionStorage (cart) / localStorage (wishlist) — different durability matches spec intent (carts ephemeral, wishlists durable). Guest hydration deferred to `queueMicrotask` to satisfy React 19's `no-sync-setState-in-effect` lint rule. Navbar dropped `wishlistCount`/`cartCount` props and now reads `useCart()` + `useWishlist()`; live curl-verified that adding 2× of a variant returns `{count:2}` and the badge updates. **/products listing** (added beyond original scope): server-rendered grid with `?category=<slug>` filtering, category chip row, dynamic `generateMetadata`, featured-first ordering, Featured/Sold-out pills, empty-state card. **Routing migration:** flat-swapped every `/shop` → `/products` (navbar label "Shop" → "Products", hero CTAs, categories cards, bestsellers cards, footer links, PDP breadcrumb), then re-pointed category links to `?category=<slug>` query strings to avoid collision with the PDP route. **AuthButton** ([src/components/auth/auth-button.tsx](../src/components/auth/auth-button.tsx)) added to the navbar between Wishlist and Bag: pill style; loading-state placeholder reserves width to prevent navbar jump on hydration; authenticated → `signOut({ callbackUrl: "/" })`, unauthenticated → `<Link href="/login">` so it works without JS and honors `pages.signIn`. **Deferred:** cart drawer UI ([cart-drawer-spec.md](features/cart-drawer-spec.md)), review submission UI + `POST /api/reviews`, `ReviewImage` schema migration, WhatsApp FAB. **Caveats / known gotchas:** (1) Footer's `SHOP` array has pre-existing wrong category slugs (`tragus` should be `cartilage-jewelry`, `septum` should be `septum-jewelry`, `waistbeads` has no matching category) — links now render the empty-state card instead of 404-ing. (2) `notFound()` returns 200 in the dev server but proper 404 in prod (Next.js dev quirk). (3) After sign-out, providers don't auto-rehydrate to the guest path until next mount — flash-free for now but worth a watch.
- 2026-05-24 — **Auth Phase 2 (Credentials + register + login-page wiring)** — [features/auth-phase-2-spec.md](features/auth-phase-2-spec.md) → [src/auth.config.ts](../src/auth.config.ts), [src/auth.ts](../src/auth.ts), [src/app/api/auth/register/route.ts](../src/app/api/auth/register/route.ts), [src/app/login/page.tsx](../src/app/login/page.tsx), [.env](../.env), [.env.example](../.env.example). Split-config pattern keeps `auth.config.ts` edge-safe with a `Credentials` placeholder (`authorize: async () => null`); real validation in `auth.ts` looks up the user by lowercase email and runs `bcrypt.compare` against `User.password`, returning `{ id, email, name, image }` on success. `POST /api/auth/register` (validated end-to-end via curl: 201 happy path, 409 duplicate email, 400 on mismatched / short password / invalid email) accepts `{ name, email, password, confirmPassword }`, hashes with bcrypt, splits `name` into `firstName`/`lastName`, creates a `CUSTOMER` user. Phase 1's GitHub provider removed entirely (project is credentials-only); cleaned `AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` from `.env` + `.env.example`. `pages.signIn` set to `/login` so the proxy gate redirects to the custom page instead of NextAuth's default. **Login page wired to the real backend** ([src/app/login/page.tsx](../src/app/login/page.tsx)): login form calls `signIn("credentials", { redirect: false })` and honors `?callbackUrl=`; signup form POSTs to `/api/auth/register` then auto-signIns on 201, falling back to "please sign in" if auto-signIn fails; surfaces "Wrong email or password" on bad creds; removed the visual-only Google/GitHub/Apple social row + the dead icon components + `socialBtnClass`/`handleSocial`. Live curl verification: credentials sign-in returned 302 + `authjs.session-token` cookie, `GET /api/auth/session` resolved with the admin user including `user.id` threading through. **Decisions / gotchas:** (1) Spec said "bcryptjs (already installed)" but `bcrypt` is what's actually installed (used by the seed) — used `bcrypt` so seeded `password123` hashes verify directly; both libs produce compatible `$2b$` hashes if we ever swap. (2) `Session` table stays empty by design — `strategy: "jwt"` keeps the session in the signed/encrypted cookie, not the DB. (3) Adding/removing `proxy.ts` requires a dev-server restart — Next.js detects proxy files at boot, not via HMR.
- 2026-05-24 — **Auth Phase 1 (NextAuth v5 baseline)** — [features/auth-phase-1-spec.md](features/auth-phase-1-spec.md) → [src/auth.config.ts](../src/auth.config.ts), [src/auth.ts](../src/auth.ts), [src/app/api/auth/[...nextauth]/route.ts](../src/app/api/auth/%5B...nextauth%5D/route.ts), [src/proxy.ts](../src/proxy.ts), [src/types/next-auth.d.ts](../src/types/next-auth.d.ts). Split config pattern: `auth.config.ts` carries providers (GitHub) + `authorized` callback (edge-safe, no adapter); `auth.ts` spreads it and adds `PrismaAdapter(prisma)`, `session: { strategy: "jwt" }`, plus `jwt`/`session` callbacks to thread `user.id` into the session. Proxy uses the wrapping pattern `export const proxy = auth(() => {})` with matcher `["/account/:path*", "/admin/:path*"]` — placeholder segments since no protected routes exist yet. NextAuth's default sign-in page used for Phase 1 (no `pages.signIn` override). `AUTH_SECRET` generated and added to `.env`; `.env.example` documents `AUTH_SECRET` / `AUTH_GITHUB_ID` / `AUTH_GITHUB_SECRET`.
- 2026-05-24 — **Full Prisma Seed** — [features/seed-spec.md](features/seed-spec.md) → [prisma/seed.ts](../prisma/seed.ts). Single-file seed covering all 16 models in dependency order. Dev login: `admin@sherryberries.test` / `password123`. New deps: `@faker-js/faker`, `bcrypt`, `@types/bcrypt`.
- 2026-05-17 — **Floating Pill Navbar** — [features/navbar-spec.md](features/navbar-spec.md) → [src/components/layout/navbar.tsx](../src/components/layout/navbar.tsx), styles in [src/app/globals.css](../src/app/globals.css), mounted in [src/app/layout.tsx](../src/app/layout.tsx).
- 2026-05-17 — **Hero Section** — [features/hero-section-spec.md](features/hero-section-spec.md) → [src/components/home/hero.tsx](../src/components/home/hero.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-17 — **Categories Grid** — [features/categories-grid-spec.md](features/categories-grid-spec.md) → [src/components/home/categories.tsx](../src/components/home/categories.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-17 — **Bestsellers Product Grid** — [features/bestsellers-grid-spec.md](features/bestsellers-grid-spec.md) → [src/components/home/bestsellers.tsx](../src/components/home/bestsellers.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Brand Story** — [features/brandstory-spec.md](features/brandstory-spec.md) → [src/components/home/brand-story.tsx](../src/components/home/brand-story.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Learn / FAQ Accordion** — [features/faq-spec.md](features/faq-spec.md) → [src/components/home/faq.tsx](../src/components/home/faq.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Berry List Newsletter** — [features/newsletter-spec.md](features/newsletter-spec.md) → [src/components/home/newsletter.tsx](../src/components/home/newsletter.tsx), styles in [src/app/globals.css](../src/app/globals.css).
- 2026-05-18 — **Footer** — [features/footer.md](features/footer.md) → [src/components/layout/footer.tsx](../src/components/layout/footer.tsx), styles in [src/app/globals.css](../src/app/globals.css), mounted in [src/app/layout.tsx](../src/app/layout.tsx).
- 2026-05-19 — **Prisma 7 + Neon PostgreSQL** — [database-spec.md](database-spec.md), [backend-architecture.md](backend-architecture.md) → [prisma/schema.prisma](../prisma/schema.prisma), [prisma.config.ts](../prisma.config.ts), [src/lib/db/index.ts](../src/lib/db/index.ts), [prisma/seed.ts](../prisma/seed.ts).
- 2026-05-19 — **Login Page** — [features/login-page-spec.md](features/login-page-spec.md) → [src/app/login/page.tsx](../src/app/login/page.tsx).
- 2026-05-20 — **Tailwind v4 migration (cross-cutting)** — All 9 components refactored from BEM-style custom CSS to Tailwind v4 utility classes inline. [src/app/globals.css](../src/app/globals.css) shrunk **2,118 → 106 lines**.
